# 审计请求 — ARCH_REVIEW
- **优先级**: HIGH
- **审计对象**: GiT/ 完整代码架构

## 审计要求

### 1. ViT 输入审查
- GiT 的 ViT 中 q, k, v 分别由什么构成？
- GiT 有三层视野（grid, win, 全局），目前 occ 任务在这三层视野的输入是否全部来自 DINOv3？还是有些层用了其他来源？
- 如果全部来自 DINOv3，是否有优化空间？

### 2. DINOv3 特征层选择
- 目前输入 GiT 的是 DINOv3 的第几层特征？
- 这个选择是否合理？有没有实验或理论依据？
- 不同层的特征有什么区别（低层细节 vs 高层语义），对 occ 任务哪个更重要？

### 3. 架构层面性能提升方案
- 从模型架构角度（不是 loss 调参），有哪些可行的方法提升 occ 预测性能？
- 需要具体方案、预估工作量、风险评估

### 4. BUG 状态核实
- 请通读 GiT/ 代码和 MASTER_PLAN.md 中的 BUG 列表，逐一核实 BUG-1 到 BUG-12 的实际修复状态
- 标记为 FIXED 的是否真的在代码中修复了？UNPATCHED 的是否确实未修？

### 5. 历史分析检查
以下 4 个分析方案是否已在当前代码中实现或部分实现？请逐一核实：
- 分析 1: Grid 分配逻辑审计（AABB → 旋转多边形覆盖）
- 分析 2: 2D-3D 视觉对齐（2D 投影框权重调制）
- 分析 3: Center/Around 采样策略（center cell 高权重）
- 分析 4: Token 与类别优化（合并 marker+class）

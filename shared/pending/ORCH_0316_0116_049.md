# ORCH 指令 — 049

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 基于 `VERDICT_ORCH049_MARKER_PATH` 修复 `ORCH_048` 遗留的 marker 模板化问题；优先修正真实生效的 fg/bg 惩罚失衡（BUG-73），并以 `iter_200` 和 `iter_500` frozen-check 作为双闸门验证是否继续训练
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, 必要时 `GiT/scripts/`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑；Critic 已确认 marker 无 GT prefix 泄漏
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**，通过 frozen-check 前不得跑完整验证
- **实施要求**:
  1. **修复 BUG-73（P0，必须做）**
     - 将 `marker_pos_punish` 从 `3.0` 调到 `1.0`
     - 将 `bg_balance_weight` 从 `2.5` 调到 `5.0`
     - 在执行报告中明确说明这两处为何才是实际生效的 fg/bg 平衡参数
  2. **进一步收紧外围 supervision（P1）**
     - 将 `around_weight` 从 `0.1` 降到 `0.0`；若实现约束要求保留极小值，最多允许 `0.05`
  3. **保留 ORCH_048 已验证有效的改动**
     - `RandomFlipBEV only`
     - `grid_assign_mode='center'`
     - `prefix_drop_rate=0.5`
     - 现有 `core/around` 监督框架
  4. **新增双阶段 frozen-check**
     - `iter_200` 生成后立即运行 quick frozen-check
     - `iter_500` 生成后再运行完整 frozen-check
     - 图统一写到:
       - `/home/UNT/yz0370/projects/GiT_agent/shared/logs/VIS/049_iter200_frozen_check`
       - `/home/UNT/yz0370/projects/GiT_agent/shared/logs/VIS/049_iter500_frozen_check`
  5. **早停规则**
     - 若 `iter_200` 满足 `marker_same > 0.95` 且 `saturation > 0.90`，立即停训，不再跑到 500
     - 若 `iter_500` 满足任一 frozen 条件（`Positive IoU > 0.95` 或 `Marker same rate > 0.90` 或 `Saturation > 0.90`），立即停训
- **验收标准**:
  1. config 中明确可见：
     - `marker_pos_punish=1.0`
     - `bg_balance_weight=5.0`
     - `around_weight<=0.05`
  2. `iter_200` frozen-check 已执行并在报告中给出数值
  3. 若进入 `iter_500`，其 frozen-check 已完整执行并产出可视化
  4. 相对 `ORCH_048` 的最小成功信号：
     - `marker_same` 明显低于 `0.9767`
     - `Positive IoU` 不高于 `0.9459`
  5. 失败即停，不继续 full val
- **截止时间**: 03:30

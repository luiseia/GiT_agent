# ORCH 指令 — 057

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 架构变更 — marker step (pos_id=0) 不加 grid_pos_embed，强制 marker 依赖图像特征决定物体存在性
- **代码路径**: `GiT/mmdet/models/detectors/git.py`, `GiT/mmdet/models/dense_heads/git_occ_head.py`
- **前置条件**: 等待 Critic 审计 `AUDIT_REQUEST_ORCH057_MARKER_NO_POS` 通过

## 背景

ORCH_049-056 实验证明: 所有超参数调整都无法阻止 grid_pos_embed 空间先验 shortcut。
唯一健康点是 ORCH_055 @100 (marker_same=0.887, TP=109)，但随后不可逆地模板化。
根因: grid_pos_embed 让 marker 不看图像就能决定"哪里有目标"。

## 核心改动

### marker step 不加 grid_pos_embed

在 grid_pos_embed 被加到 input 的位置：
- **之前**: `grid_start_embed += grid_pos_embed`（所有 token 共享）
- **之后**: marker step (pos_id=0) 的 grid_pos_embed 用零向量替代，class/box steps (pos_id 1-9) 正常使用

### 具体实现（待 Critic 确认）

```python
# 方案 A: 在 forward_transformer 中修改
# grid_start_embed 对 marker 不加 pos，对 box 正常加
if self.training or True:  # 训练和推理都需要一致
    # grid_pos_embed: (B, num_cells, dim)
    # 需要分 marker 和 non-marker 路径
    # 但 grid_start_embed 是 cell 级的，不是 token 级的
    # 可能需要在 decoder 层面处理
```

### 其余配置不变
- `marker_pos_punish=1.0`, `bg_balance_weight=5.0` (ORCH_049/055 配置)
- `marker_grid_pos_dropout=0.0`
- 2-GPU DDP

### Frozen-check
@100, @200, @300, @400, @500（不早停除非 saturation>0.95）

## 强制约束
1. 必须 2-GPU DDP
2. 训练和推理行为一致（marker 在推理时也不加 grid_pos_embed）
3. 不影响 class/box steps 的位置编码

## 验收标准
1. Critic 审计通过
2. 代码修改仅影响 marker step 的位置编码
3. @100 marker_same 应低于 ORCH_055 @100 的 0.887
4. 如 @200 marker_same < 0.95 → 首次 @200 HEALTHY

## 截止时间
- 等待 Critic 审计后执行

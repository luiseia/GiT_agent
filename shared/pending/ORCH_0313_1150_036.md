# ORCH 指令 — 036: score_thr 消融实验 (@12000 checkpoint)
- **状态**: COMPLETED (superseded by ORCH_040 — scores were all 1.0, ablation meaningless)
- **优先级**: HIGH
- **目标**: 在 @12000 checkpoint 上执行 score_thr 消融 (0.1/0.2/0.3/0.5), 评估后处理对 FP 的抑制效果
- **截止时间**: 03/13 14:00

## 背景
- 三次 Critic VERDICT (@8k/@10k/@12k) 均要求执行 score_thr 消融, 至今未完成
- @12k car_P=0.100 (历史最佳), bg_FA=0.2831
- 零训练成本, 仅需推理 eval
- 结果对 BUG-17 修复策略有直接参考价值

## 执行步骤

### 1. 确认 @12000 checkpoint 存在
```bash
ls -la /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v4/iter_12000.pth
```

### 2. 对每个 score_thr 值运行 eval
对 score_thr ∈ {0.1, 0.2, 0.3, 0.5}:

修改 config 或通过 `--cfg-options` 传入 `model.bbox_head.test_cfg.score_thr=X`

```bash
cd /home/UNT/yz0370/projects/GiT
conda activate GiT

# score_thr=0.1
python -m torch.distributed.launch --nproc_per_node=1 --master_port=29501 \
  tools/test.py configs/GiT/plan_full_nuscenes_multilayer.py \
  /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v4/iter_12000.pth \
  --launcher pytorch \
  --cfg-options model.bbox_head.test_cfg.score_thr=0.1

# 重复 0.2, 0.3, 0.5
```

**注意**: 使用 1 GPU 运行, 不干扰训练 (训练用 GPU 0-3)。如果 4 GPU 全被训练占用, 等训练空闲期或用 `CUDA_VISIBLE_DEVICES=` 指定。

### 3. 收集结果
记录每个 score_thr 下的:
- car_R, car_P, bg_FA
- 其他活跃类 (bus, truck) 的 R/P
- 总 FP 数估算

### 4. 写报告
输出到 `shared/logs/report_ORCH_036.md`, 格式:

| score_thr | car_R | car_P | bg_FA | bus_R | bus_P | 总结 |
|-----------|-------|-------|-------|-------|-------|------|

## 预期
- score_thr=0.2-0.3 可能大幅降低 bg_FA 而代价较小
- 如果 score_thr=0.2 就能把 bg_FA 从 0.28 降到 <0.15, 则 BUG-17 紧迫性降低

# ORCH 指令 — 048

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 修正 `ORCH_047 @500` 仍然 frozen 的训练算法问题，不再增加 full val 成本；将“结果可视化中的核心/周边过滤逻辑”部署到 supervision，并从零启动新训练验证是否能摆脱 `1200/1200` 全正模板
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, `GiT/mmdet/datasets/pipelines/generate_occ_flow_labels.py`, `GiT/mmdet/models/dense_heads/git_occ_head.py`, `GiT/mmdet/models/detectors/git.py`, `GiT/scripts/`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要保留** `GlobalRotScaleTransBEV`；本轮只保留 `RandomFlipBEV`
  3. `@500` 只跑 `check_frozen_predictions.py`，**不再先跑 full val**
- **实施要求**:
  1. 在 config 中显式设置 `grid_assign_mode='center'`，不要依赖默认值
  2. 将“核心 cell / 周边 cell”的过滤逻辑从可视化迁移到 supervision：
     - 在 `GenerateOccFlowLabels` 或 occ target 构造路径中，把每个 GT 对应的 image fine cells 至少区分为 `core` 与 `around`
     - 在 target weight 中对二者使用不同权重
     - 推荐首版：`core_weight=1.0`，`around_weight=0.0~0.1`
     - 目标是保留 CEO 关心的 BEV box 属性监督，同时显著削弱周边格子把模型推向全正模板
  3. 回收前景/背景权重失衡：
     - `pos_cls_w_multiplier` 调低到 `1.0`
     - `neg_cls_w` 提高到 `1.0` 左右
     - 若实现里还存在“正样本整串 token 强压、背景仅单 token 弱压”的路径，请同步做最小必要修正
  4. 将现有 `token_drop_rate=0.3` 升级为更强的 anti-shortcut 方案：
     - 优先实现 scheduled sampling 或 prefix dropout
     - 要求它作用于 occupancy teacher forcing 主路径，而不是只做逐 token 随机替换
  5. 新训练从零启动到 `iter_500`，生成 `iter_500.pth` 后立刻运行：
     - `python scripts/check_frozen_predictions.py --config <cfg> --checkpoint <iter_500.pth> --out-dir /home/UNT/yz0370/projects/GiT_agent/shared/logs/VIS/048_iter500_frozen_check`
  6. 如果 frozen-check 失败：
     - **立即停训**
     - 不再继续 full val
- **验收标准**:
  1. 新 config 已移除 `GlobalRotScaleTransBEV`，仅保留 `RandomFlipBEV`
  2. config 中明确可见 `grid_assign_mode='center'`
  3. supervision 中已引入 `core/around` 差异权重，且不是死参数
  4. `@500` frozen-check 完整执行并产出图到 `shared/logs/VIS/048_iter500_frozen_check/`
  5. 通过标准：
     - Positive IoU `< 0.95`
     - Marker same rate `< 0.90`
     - 不得出现 `1200/1200` 全正饱和
  6. 失败标准：
     - 满足任一 frozen 条件即停止，不再跑 full val
- **截止时间**: 01:30

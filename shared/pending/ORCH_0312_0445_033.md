# ORCH 指令 — 033: Kill ORCH_032 + 修复重启多层训练
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: 修复 BUG-57/58/59/60, 重启多层特征训练
- **截止时间**: 03/12 06:00
- **依据**: Critic VERDICT_MULTILAYER_032_COLLAPSE (STOP), 方案 A+B 混合

## 背景

ORCH_032 @2000 全面坍缩 (car=0, 仅 ped_R=0.83)。根因:
- BUG-57: proj.0 零学习 (8:1 随机投影摧毁信息)
- BUG-58: load_from=None 无暖启动
- BUG-59: 16384→2048 压缩比太激进
- BUG-60: clip_grad=10.0 有效 lr 仅 12.5%

## Task 1: Kill ORCH_032

1. Kill 训练进程: `kill $(pgrep -f "plan_full_nuscenes_multilayer.py")`
2. 确认所有 GPU 进程已释放 (nvidia-smi)
3. **保留 ORCH_032 的 checkpoint 和日志** (iter_2000.pth 用于调试对比)

## Task 2: 修改 config

修改 `configs/GiT/plan_full_nuscenes_multilayer.py`，以下 4 处改动:

### 改动 1: load_from (BUG-58 修复)
```python
# 旧值:
load_from = None
# 新值:
load_from = '/mnt/SSD/GiT_Yihao/Train/Train_20260311/full_nuscenes_filtered/iter_2000.pth'
```
注意: mmdet `load_checkpoint(strict=False)` 会自动跳过 shape 不匹配的 proj.0 权重。

### 改动 2: preextracted_proj_hidden_dim (BUG-59 修复)
```python
# 旧值:
preextracted_proj_hidden_dim=2048,
# 新值:
preextracted_proj_hidden_dim=4096,
```
压缩比从 8:1 降至 4:1。

### 改动 3: clip_grad (BUG-60 修复)
```python
# 旧值:
clip_grad=dict(max_norm=10.0, norm_type=2),
# 新值:
clip_grad=dict(max_norm=30.0, norm_type=2),
```

### 改动 4: lr_mult for proj (BUG-57 加速收敛)
在 `paramwise_cfg` 的 `custom_keys` 中找到 proj 相关的 `lr_mult`：
```python
# 旧值:
'backbone.patch_embed.proj': dict(lr_mult=2.0),
# 新值:
'backbone.patch_embed.proj': dict(lr_mult=5.0),
```

### 验证改动
修改后运行:
```bash
cd /home/UNT/yz0370/projects/GiT
python -c "
from mmengine.config import Config
cfg = Config.fromfile('configs/GiT/plan_full_nuscenes_multilayer.py')
print('load_from:', cfg.load_from)
print('proj_hidden_dim:', cfg.model.backbone.preextracted_proj_hidden_dim)
print('clip_grad:', cfg.optimizer_config.grad_clip)
proj_key = [k for k in cfg.optimizer.paramwise_cfg.custom_keys if 'proj' in k]
for k in proj_key:
    print(f'{k}: {cfg.optimizer.paramwise_cfg.custom_keys[k]}')
"
```

## Task 3: 启动 ORCH_033

- 创建工作目录: `mkdir -p /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v2`
- 启动训练:
```bash
cd /home/UNT/yz0370/projects/GiT
CUDA_VISIBLE_DEVICES=0,1,2,3 nohup python -m torch.distributed.launch \
  --nproc_per_node=4 \
  tools/train.py configs/GiT/plan_full_nuscenes_multilayer.py \
  --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v2 \
  --launcher pytorch \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v2/train_stdout.log 2>&1 &
```

## Task 4: 验证启动

等待 2 分钟后确认:
1. 4 个 GPU 进程正常启动 (nvidia-smi)
2. 检查 mmengine 日志中的 **unexpected/missing keys** 输出:
   - **预期**: `backbone.patch_embed.proj.0.weight` 和 `backbone.patch_embed.proj.0.bias` 因 shape 不匹配被跳过
   - **预期**: 其余 backbone layers、OCC head 正常加载
3. 确认 iter 1 的 grad_norm < 100 (clip_grad=30 应能覆盖)
4. 确认 VRAM 使用 (proj_hidden=4096 预期 +2-3GB vs ORCH_032)
5. 如 VRAM > 42GB/GPU → 报告但不停止

## 验收标准
- ORCH_032 已停止
- Config 4 处修改已验证
- ORCH_033 训练正常运行, partial load 正确 (仅 proj.0 跳过)
- 写报告到 `shared/logs/report_ORCH_033.md`

# ORCH 指令 — 037: BUG-17 Weight Cap 修复 + Mini 验证
- **状态**: COMPLETED (code committed, mini validation pending GPU)
- **优先级**: CRITICAL
- **目标**: 实现 BUG-17 weight cap (max_w=3.0) 修复, 在 mini 数据上验证不引起坍缩
- **截止时间**: 03/13 16:00

## 背景
- BUG-17 per-batch sqrt balance 导致类别 FP 竞争, 已在 @10k/@12k 产生 bus 922K FP
- 总 FP 从 1.88M→2.06M 持续上升
- Critic 推荐方案 A: Weight Cap max_w=3.0 (一行改动, 最低风险)
- @16k eval 后立即部署

## 修复位置

文件: `GiT/mmdet/models/dense_heads/git_occ_head.py`
约 L832 附近, `_class_weights` 计算逻辑:

```python
# 当前代码
_class_weights[c] = 1.0 / _math.sqrt(cnt / _min_count)

# 修改为 (添加 weight cap)
_class_weights[c] = min(1.0 / _math.sqrt(cnt / _min_count), 3.0)
```

## 执行步骤

### 1. 定位代码
```bash
grep -n "sqrt" /home/UNT/yz0370/projects/GiT/mmdet/models/dense_heads/git_occ_head.py
```

### 2. 实现修复
- 添加 config 参数 `max_class_weight=3.0` (默认值, 向后兼容)
- 当 `max_class_weight > 0` 时启用 cap
- 代码修改后确认语法正确

### 3. Mini 数据验证
```bash
cd /home/UNT/yz0370/projects/GiT
conda activate GiT

# 用 mini 数据跑 2000 iter, 使用 1 GPU
python -m torch.distributed.launch --nproc_per_node=1 --master_port=29502 \
  tools/train.py configs/GiT/plan_mini_nuscenes_multilayer.py \
  --cfg-options model.bbox_head.max_class_weight=3.0 \
  --launcher pytorch
```

**注意**: 使用 1 GPU, 不干扰主训练。

### 4. 验证标准
- 无坍缩 (loss 不发散, car_R > 0)
- 类别 recall 分布更均匀 (无极端 FP 集中)
- 小类仍有非零 recall (证明 cap 不过度压制)

### 5. 写报告
输出到 `shared/logs/report_ORCH_037.md`:
- 修改的代码 diff
- Mini 训练曲线
- @2000 eval 结果 (如有)
- 是否可安全部署到 full nuScenes

## BUG-17 修复备选方案 (仅在方案 A 失败时)
- B: Dataset-level sqrt (固定权重, 中等工作量)
- C: 关闭 sqrt (核选项, 小类可能不激活)

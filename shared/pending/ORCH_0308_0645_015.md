# ORCH 指令 — 015
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: P6 诊断实验 — 单类 car + 宽中间层
- **背景**: VERDICT_P6_ARCHITECTURE 要求先做诊断确认瓶颈来源。P5b 即将完成 (98.5%), GPU 即将释放。BUG-26 确认仅用 CAM_FRONT。

## 任务清单

### 1. 创建 plan_k_car_only_diag.py (实验 α)
- 基于 `plan_i_p5b_3fixes.py`
- **关键修改**:
  - `classes = ["car"]`, `num_classes = 1`
  - `num_vocal` 重新计算: 168 (bins) + 1 (cls) + 1 (bg) + 4 (markers) + 36 (θ_G) + 10 (θ_F) + 1 (ignore) = **221**
  - `load_from`: P5b@3000 checkpoint (路径: 查找 `/mnt/SSD/GiT_Yihao/Train/` 下 P5b 的 iter_3000.pth)
  - `max_iters = 2000` (足够观察趋势)
  - `val_interval = 500`
  - 其余参数保持不变 (双层投影, sqrt 权重, mini 数据集)
- 注意: checkpoint 是 4 类训练的, 加载到 1 类 config 时 vocab_embed 动态重建, 无兼容问题

### 2. 创建 plan_l_wide_proj_diag.py (实验 β)
- 基于 `plan_i_p5b_3fixes.py`
- **关键修改**:
  - `preextracted_proj_hidden_dim = 2048` (当前 1024)
  - 找到投影层配置位置 (可能在 `model.backbone` 或 head config 中)
  - `load_from`: P5b@3000 checkpoint
  - `max_iters = 2000`
  - `val_interval = 500`
  - `classes` 保持 4 类 (与 P5b 一致, 控制变量)
- **注意**: 中间层从 1024→2048, 预训练权重 shape 不匹配, 需要确认加载时是否自动跳过投影层权重 (预期: 是, 因为 shape mismatch 会被 mmdet 的 load_checkpoint 自动跳过)

### 3. 验证 BUG-26: 确认只用 CAM_FRONT
- 在 `generate_occ_flow_labels.py` 中确认: 只有 CAM_FRONT 用到 DINOv3 特征
- 验证路径: 搜索所有使用 `preextracted_feature` 的代码, 确认 6 个相机中只有 CAM_FRONT

### 4. P5b 完成后启动实验
- 确认 P5b@6000 最终 val 数据 (预期与 @5500 几乎相同)
- GPU 释放后, 同时启动两个诊断实验:
  - 实验 α (plan_k): GPU 0
  - 实验 β (plan_l): GPU 2
  - (各用单 GPU, 2000 iter 约 1-2 小时)
- 记录启动命令和 PID

### 5. 结果报告
- 两个实验完成后, 汇总 val 数据到 `shared/logs/admin_report_diag.md`
- 重点对比:
  - α car_P vs β car_P vs P5b@3000 car_P (0.107)
  - α off_th vs β off_th vs P5b@3000 off_th (0.200)
  - 阈值: 如果 α car_P > 0.15 → 类竞争是瓶颈

- **代码路径**: GiT/configs/GiT/, GiT/mmdet/models/dense_heads/, GiT/mmdet/datasets/
- **验收标准**:
  1. plan_k 和 plan_l 创建并通过 Python 语法检查
  2. BUG-26 验证结果
  3. 两个诊断实验启动并完成
  4. 结果汇总报告
- **截止时间**: P5b 完成后 3 小时内

# ORCH 指令 — 060
- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 在 GiT 中新增统一可视化，逐 `cell/slot` 对齐 `prediction` 与训练时 `target assignment`，用于排查 occ head 中 target 分配错误。
- **代码路径**: GiT/scripts/visualize_pred_target_alignment.py 或等效新脚本；可复用 `scripts/visualize_pred_vs_gt.py` 与 `ssd_workspace/Debug/debug_viz_occhead.py` 的逻辑。
- **验收标准**:
  1. 对同一样本同时输出图像网格叠加图与 BEV 图。
  2. 明确展示每个 `cell/slot` 的 `assigned GT id / target marker / target class / target box` 与 `pred marker / pred class / pred box / score`。
  3. 至少提供一个 `mismatch-only` 视图，能快速看出 target/pred 不一致位置。
  4. 产出脚本说明、示例命令、输出目录，并将结果写入 `shared/logs/report_ORCH_0316_1845_060.md`。
  5. 若发现现有 target 生成链路中存在明确异常或歧义，报告中单独列出。
- **截止时间**: 20:15

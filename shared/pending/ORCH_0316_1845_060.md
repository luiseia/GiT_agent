# ORCH 指令 — 060
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: 用 occ head 真实 target 生成链路做可视化排查。必须基于 `GiT/mmdet/models/dense_heads/git_occ_head.py` 中 `_get_targets_single_based_on_bev` 的实际输出反编 target，可视化后再对同一样本执行预测，并逐 `cell/slot` 对齐 `target` 与 `prediction`。
- **代码路径**: `GiT/mmdet/models/dense_heads/git_occ_head.py`、`GiT/scripts/visualize_pred_target_alignment.py` 或等效新脚本；可复用现有 `scripts/visualize_pred_vs_gt.py`，但不能只做 `pred vs GT`。
- **强制方法**:
  1. 不要手工重写一套近似 assignment 逻辑替代训练链路。
  2. 必须直接调用或 hook `_get_targets_single_based_on_bev`，拿到其真实 target 输出，再做“反编/解码”成可视化对象。
  3. 在得到 target 可视化之后，再对同一个 `sample_idx` 跑模型预测，并建立 `cell -> slot -> target/pred` 的一一对应。
- **验收标准**:
  1. 对同一样本同时输出图像网格叠加图与 BEV 图。
  2. 明确展示每个 `cell/slot` 的 `assigned GT id / target marker / target class / target box`，这些内容必须来自 `_get_targets_single_based_on_bev` 的真实输出反编。
  3. 同图或同报告中展示对应 `pred marker / pred class / pred box / score`。
  4. 至少提供一个 `mismatch-only` 视图，能快速看出 target/pred 不一致位置。
  5. 报告里必须写清楚：target 是如何从 `_get_targets_single_based_on_bev` 输出反编出来的；使用了哪个样本；如何保证 prediction 与 target 是同一样本、同一网格、同一 slot。
  6. 产出脚本说明、示例命令、输出目录，并将结果写入 `shared/logs/report_ORCH_0316_1845_060.md`。
  7. 若发现 target 生成链路中存在明确异常、歧义或难以反编的编码设计，报告中单独列出。
- **截止时间**: 20:15

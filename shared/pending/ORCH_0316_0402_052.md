# ORCH 指令 — 052

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: FG/BG 比精确调参 — 1x(@500全阴性) 和 3.3x(@200全正) 之间取 1.67x
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**
  5. **grid_pos_embed dropout 保持 0.0**（BUG-77 已确认有害）

## 背景

| ORCH | marker_pos_punish | bg_balance_weight | FG/BG | @200 结果 |
|------|-------------------|-------------------|-------|-----------|
| 049 | 1.0 | 5.0 | 1x | TP=112 (唯一存活), @500 全阴性 |
| 051 | 2.0 | 3.0 | 3.3x | 全正饱和 1200/1200 |
| **052** | **1.0** | **3.0** | **1.67x** | **?** |

## 实施要求

### Config 改动
- `marker_pos_punish`: `2.0` → **`1.0`**（回到 ORCH_049 值）
- `bg_balance_weight`: 保持 **`3.0`**
- FG/BG = (5 × 1.0) / (3.0 × 1.0) = **1.67x**
- `marker_grid_pos_dropout`: 保持 **`0.0`**

### 其余配置不变
- `around_weight=0.0`, `grid_assign_mode='center'`, `RandomFlipBEV only`, `prefix_drop_rate=0.5`

### 双阶段 frozen-check + 早停 (OR 触发)

@200: `marker_same > 0.97` 或 `saturation > 0.90` 或 `saturation < 0.05` → STOP
@500: `Positive IoU > 0.95` 或 `Marker same > 0.90` 或 `Saturation > 0.90/<0.05` → STOP

可视化: `shared/logs/VIS/052_iter{200,500}_frozen_check`

## 验收标准

1. Config: `marker_pos_punish=1.0`, `bg_balance_weight=3.0`, `marker_grid_pos_dropout=0.0`
2. @200 frozen-check 执行
3. **期望**: @200 类似 ORCH_049 (TP>0, saturation 0.1-0.8)
4. **关键验证**: @500 是否避免全阴性崩塌 — 若 saturation 仍 0.1-0.8 则首次通过!
5. 失败即停

## 截止时间
- 06:00 CDT

# ORCH_044: 多层 ViT-L (frozen) + LN + 投影，从 iter_6000 加载

- **状态**: COMPLETED
- **优先级**: P0 — 立即执行
- **签发时间**: 2026-03-15 01:30
- **触发**: @6000 eval 确认 P2+P3 有效 (car_R 0→0.582) + CEO 指令

## 背景

P2+P3 修复成功消除 frozen predictions (car_R=0.582)。CEO 指令: 实施冻结多层 LN/BN ViT-L。
代码已完整支持多层提取 (OnlineDINOv3Embed)，只需修改 config。

## 任务 (严格按顺序)

### Step 1: 停止当前训练
```bash
ps aux | grep train.py | grep -v grep
kill <PID>  # PID 1444318 或相关进程
nvidia-smi  # 确认 GPU 释放
```

### Step 2: 修改 Config
文件: `configs/GiT/plan_full_nuscenes_large_v1.py`

#### 2a. 多层提取 (单层→四层)
```python
# 旧:
online_dinov3_layer_idx=23,
# 新:
online_dinov3_layer_indices=[5, 11, 17, 23],    # ViT-L 24层, 4层均匀采样, 4×1024=4096维
# 注释掉 online_dinov3_layer_idx 行
```

#### 2b. 投影层 (1024→1024 改为 4096→2048→GELU→1024)
```python
# 旧:
preextracted_proj_hidden_dim=None,
preextracted_proj_use_activation=False,
# 新:
preextracted_proj_hidden_dim=2048,               # 隐藏层 4096→2048→GELU→1024
preextracted_proj_use_activation=True,            # GELU 激活
```

#### 2c. 保持冻结
```python
online_dinov3_unfreeze_last_n=0,                  # 不变，保持冻结
```

#### 2d. 加载权重 (注意: 必须 resume=False)
```python
load_from = '/mnt/SSD/GiT_Yihao/Train/Train_20260314/full_nuscenes_large_v1/iter_6000.pth'
resume = False  # ⚠️ 架构变化，不能 resume optimizer — 只加载匹配的权重
```
注意: `resume=False` 意味着训练从 iter_0 重新开始计数，但模型权重从 iter_6000 加载。投影层 (4096→2048→1024) 会随机初始化（因为形状不匹配旧的 1024→1024）。

### Step 3: 启动训练
```bash
cd /home/UNT/yz0370/projects/GiT
# 确认 GPU 可用
nvidia-smi

# 启动 (2 GPU 或 4 GPU)
nohup bash -c 'CUDA_VISIBLE_DEVICES=0,2 PORT=29500 \
  ./tools/dist_train.sh configs/GiT/plan_full_nuscenes_large_v1.py 2' \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260314/nohup_large_v1_multilayer.out 2>&1 &

echo "PID: $!"
```

### Step 4: 验证
1. 确认训练从 iter 0 (或 1) 开始
2. 确认日志中显示多层特征加载 (4 intermediate layers)
3. 确认显存合理 (~27-30GB/GPU)
4. 前 10 iter 的 loss 记录

## 报告
写报告到 `shared/logs/report_ORCH_044.md`，包含:
- 停止旧训练的 PID
- Config diff
- 新训练 PID
- 前 20 iter loss/grad_norm
- 显存使用

## 注意事项
- DINOv3 内部已对每层输出做 LayerNorm (`norm=True`)，不需要额外 LN
- 投影层随机初始化是预期的，前几百 iter loss 会偏高
- 日志用新文件名 `nohup_large_v1_multilayer.out`
- work_dir 不变，checkpoint 会继续保存到同一目录

# ORCH 指令 — 059

- **状态**: COMPLETED (FAIL — all-negative collapse, pos_slots 259→7→23, marker_same>0.96 全程)
- **优先级**: HIGH
- **目标**: 修复 BUG-82 — 添加 marker_init_bias 使初始 P(BG)=80%，打破 all-positive 初始化
- **代码路径**: `GiT/mmdet/models/dense_heads/git_occ_head.py`
- **前置条件**: Critic 审计 `VERDICT_ORCH059_LOSS_INIT` 已通过 (CONDITIONAL)

## ⚠️ 严格单变量实验

**仅做 BUG-82 修复，不做任何其他改动：**
- ❌ 不改 bg_balance_weight（保持 5.0）
- ❌ 不改 marker_pos_punish（保持 1.0）
- ❌ 不开启 Focal Loss（BUG-81 focal_alpha 方向错误未修正）
- ❌ 不改 marker_no_grid_pos（保持 False/关闭）
- ❌ 不改 LR（保持 5e-5）

## 代码修改（~5 行）

### 1. `git_occ_head.py` __init__（L214 之后）:
```python
import math
# BUG-82 fix: RetinaNet-style prior P(bg)≈0.80
self.marker_init_bias = nn.Parameter(torch.tensor([-2.0, -2.0, -2.0, +0.5]))
```

### 2. `git_occ_head.py` loss_by_feat_single — 训练路径（L857 和 L931）:
在 marker_logits 计算后加 bias:
```python
marker_logits = logits_flat[:, 0, marker_start : marker_start + 4] + self.marker_init_bias
```

### 3. `git_occ_head.py` decoder_inference — 推理路径（L1155）:
```python
marker_logits = logits[:, self.marker_near_id : self.marker_near_id + 4] + self.marker_init_bias
```

**注意**: 训练和推理两处都必须加，保持一致性。

## Config（不改，保持 ORCH_055 配置）
- `marker_pos_punish=1.0`, `bg_balance_weight=5.0`
- `marker_no_grid_pos=False`（关闭）
- `marker_grid_pos_dropout=0.0`
- 2-GPU DDP（必须）

## 训练启动

```bash
CUDA_VISIBLE_DEVICES=2,3 python -m torch.distributed.launch \
  --nproc_per_node=2 --master_port=29512 \
  tools/train.py configs/GiT/plan_full_nuscenes_large_v1.py \
  --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260316/full_nuscenes_large_v1_orch059 \
  --launcher pytorch
```

## 多点 frozen-check

@100, @200, @300, @400, @500 — 仅 saturation>0.95 早停

## 验收标准

1. 代码中 `marker_init_bias` 在 __init__、训练、推理三处正确添加
2. @100: `marker_same < 0.887`（优于 ORCH_055 @100）
3. @200: `marker_same < 0.95`（首次 @200 HEALTHY）
4. @500: `marker_same < 0.95` 且 `diff/Margin > 20%`（终极目标）
5. 失败即停

## 截止时间
- 18:00 CDT

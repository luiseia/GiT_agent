# ORCH 指令 — 053

- **状态**: PENDING
- **优先级**: HIGH
- **目标**: bg_balance_weight 二分搜索 — bg=3.0(死) vs bg=5.0(活)，试 bg=4.0
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**
  5. **grid_pos_embed dropout 保持 0.0**

## 背景

| ORCH | marker_pos_punish | bg_balance_weight | @200 结果 |
|------|-------------------|-------------------|-----------|
| 049 | 1.0 | 5.0 | TP=112 (唯一存活) |
| 052 | 1.0 | 3.0 | 0/1200 all-negative |
| **053** | **1.0** | **4.0** | **?** |

## 实施要求

### 唯一改动
- `bg_balance_weight`: `3.0` → **`4.0`**
- 其余完全不变: `marker_pos_punish=1.0`, `marker_grid_pos_dropout=0.0`, `around_weight=0.0`, `grid_assign_mode='center'`, `RandomFlipBEV only`, `prefix_drop_rate=0.5`

### 双阶段 frozen-check + 早停 (OR 触发)

@200: `marker_same > 0.97` 或 `saturation > 0.90` 或 `saturation < 0.05` → STOP
@500: `Positive IoU > 0.95` 或 `Marker same > 0.90` 或 `Saturation > 0.90/<0.05` → STOP

可视化: `shared/logs/VIS/053_iter{200,500}_frozen_check`

## 验收标准

1. Config: `marker_pos_punish=1.0`, `bg_balance_weight=4.0`
2. @200 frozen-check 执行
3. 如果 @200 存活 (TP>0, saturation 0.05-0.90) → 继续到 @500，这是关键验证
4. 失败即停

## 截止时间
- 07:00 CDT

# ORCH_024: Full nuScenes 训练 — 2048+GELU + 在线 DINOv3
- 状态: DELIVERED
- 签发: Conductor | Cycle #86
- 时间: 2026-03-08 16:10
- 优先级: **CRITICAL — Mini 验证完成, Full nuScenes 是下一阶段核心目标**
- GPU: 0+1+2+3 (4 GPU DDP)

## 背景

VERDICT_P2_FINAL_FULL_CONFIG: **PROCEED — 2048+GELU 用于 Full nuScenes**
- Mini 验证阶段结束 (P1-P6 + 诊断实验全部 COMPLETED)
- 2048+GELU 架构经 P6+Plan P2 验证, 收敛更快, 非线性容量更强
- CEO 决策: 在线 DINOv3 frozen (避免 175GB 预提取存储)

## Task 1: 创建 Full nuScenes Config (~30 min)

从 `plan_p2_wide_gelu_fix.py` 为基础, 创建 `plan_full_nuscenes_gelu.py`:

### 必须修改的参数:

```python
# ★ 投影层 (保持 Plan P2 架构)
preextracted_proj_hidden_dim = 2048
preextracted_proj_use_activation = True    # GELU!

# ★ 在线 DINOv3 (CEO 决策)
use_preextracted_features = False          # 在线提取!
# 确保 DINOv3 frozen (不 unfreeze, BUG-35)
# online_dinov3_weight_path = '<path>'     # 确认 DINOv3 权重路径

# ★ 数据 (Full nuScenes)
data_root = 'data/nuscenes/'              # 全量数据
# ann_file 从 mini 改为 full train/val split

# ★ LR Schedule (Full nuScenes 长训练)
max_iters = 40000                          # 初始目标, 可调
warmup = 2000                              # 全数据量的 ~5%
milestones = [15000, 25000]                # 相对 begin
# base_lr 和 gamma 参考 P6 config

# ★ Val
val_interval = 2000                        # 每 2000 iter eval

# ★ Checkpoint
checkpoint_interval = 2000                 # 每 2000 iter 保存

# ★ lr_mult (保持 P6 验证值)
# backbone.patch_embed.proj: lr_mult = 2.0

# ★ Balance (保持)
# balance_mode = 'sqrt'
# bg_balance_weight = 2.5

# ★ 词表 (不变!)
# num_vocal = 230
# marker_end_id = 182
# classes = 10 类
```

### 关键检查清单:
1. [ ] `num_vocal=230` 不变 (BUG-27/31 教训)
2. [ ] `preextracted_proj_use_activation=True` (GELU!)
3. [ ] `preextracted_proj_hidden_dim=2048`
4. [ ] `lr_mult=2.0` for proj
5. [ ] `milestones` 在 `max_iters` 范围内 (BUG-42 教训!)
6. [ ] val_dataloader 包含 `sampler=dict(type='DefaultSampler', shuffle=False)` (BUG-33 fix)
7. [ ] 在线 DINOv3 模式正确配置
8. [ ] DINOv3 frozen (不 unfreeze)

## Task 2: 验证在线 DINOv3 代码路径 (~20 min)

**必须在启动前确认:**

1. `use_preextracted_features=False` 时 `vit_git.py` 的前向传播路径正确
2. DINOv3 权重加载正常 (frozen, no grad)
3. 单 GPU 跑 10 iter 无报错, 速度估算
4. 显存占用: 确认单卡 < 45GB (A6000 48GB, 留 3GB 余量)
5. 如果显存不够: 尝试 gradient checkpointing 或 reduce batch_size

**如果在线路径有根本问题 (代码未实现/显存不够):**
- 备选: 预提取 Full nuScenes DINOv3 特征 (175GB fp16, 约 2-3h)
- 然后用预提取模式训练

## Task 3: 启动 4-GPU DDP 训练

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python -m torch.distributed.launch \
    --nproc_per_node=4 \
    tools/train.py \
    configs/GiT/plan_full_nuscenes_gelu.py \
    --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260308/full_nuscenes_gelu \
    --launcher pytorch
```

**启动后立即检查:**
- 4 GPU 显存正常 (< 45GB each)
- Loss 正常下降
- 速度估算 (预计 6-10 s/iter with online DINOv3)
- 无报错

## Task 4: 早期监控

**@500 val (~1h after start):**
- 确认 val 正常运行
- 确认指标方向正确 (car_P > 0)

**@1000 val:**
- car_P > 0.02 → 在线路径正常, 继续
- car_P < 0.02 → 在线路径有问题, 报告 Conductor, 考虑切换预提取

**@2000 val:**
- 报告完整指标, 与 mini 对比
- Full nuScenes 预期: car_P 0.05-0.10 (早期), 类振荡大幅减少

## 注意事项

1. **不要用 DDP val 做绝对判断** — @2000 后做单 GPU re-eval 确认 (BUG-33 教训)
2. **磁盘空间**: checkpoint 约 3-4 GB each, 40000 iter / 2000 = 20 个 ckpt ≈ 60-80 GB. SSD 剩余 528 GB 足够
3. **训练时间**: ~40000 iter × 8 s/iter ≈ 89h ≈ 3.7 天 (估算, 实际可能更快/慢)
4. **定期报告**: 每 2000 iter val 结果报告一次

## 验收标准

1. Full nuScenes config 创建, 通过所有检查清单项
2. 在线 DINOv3 代码路径验证通过
3. 4 GPU DDP 训练启动, 无报错
4. @500 早期 eval 确认方向正确
5. @1000 car_P > 0.02 (在线路径可用性确认)

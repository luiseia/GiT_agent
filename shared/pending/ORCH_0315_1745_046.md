# ORCH_046: 修复 BUG-69 (adapt lr_mult) + BUG-62 (clip_grad) — 验证性训练

- **状态**: PENDING
- **优先级**: P0
- **签发时间**: 2026-03-15 17:45
- **触发**: Critic VERDICT_ORCH046_PLAN 审计通过, 确认 BUG-69 是 ORCH_045 首因

## 目标

最小改动验证: 只修复 2 个 CRITICAL config bug, 从零训练 2000 iter, 确认 adaptation layers 能正常学习且不崩塌。

## Config 修改 (plan_full_nuscenes_large_v1.py)

### 修改 1: BUG-69 — adaptation layers lr_mult

在 `paramwise_cfg.custom_keys` 中添加 (在 `'backbone'` 之前, 利用 substring longest-first match):

```python
'backbone.patch_embed.adapt_layers': dict(lr_mult=1.0),
'backbone.patch_embed.adapt_norm': dict(lr_mult=1.0),
```

### 修改 2: BUG-62 — clip_grad

```python
# L372: clip_grad=dict(max_norm=10.0) →
clip_grad=dict(max_norm=50.0, norm_type=2)
```

### 不改的部分 (本轮不动)
- PhotoMetricDistortion: 保留 (已在 config 中)
- token_drop_rate=0.3: 保留
- bert_embed: 保留 bert-base (BUG-64 推迟到 ORCH_047)
- 不加 RandomFlip3D (推迟到 ORCH_047)
- 架构不变: 多层 [5,11,17,23] + 2 层 adaptation + GiT-Large

## 训练

- 从零训练 (load_from=None, SAM pretrained via init_cfg)
- GPU 0,2 (2×A6000)
- batch_size=1, accumulative_counts=8, effective batch=16
- val@2000

## 验证指标 (@2000)

### 成功条件 (全部满足即 PROCEED):
1. bg_FA < 0.7 (vs ORCH_045 的 0.92)
2. 预测非 frozen: check_frozen_predictions.py IoU < 0.9
3. adaptation layers 参数有实质更新 (打印 lr 确认)

### 失败条件 (任一即 STOP):
1. bg_FA ≥ 0.9
2. frozen predictions 确认
3. adaptation layers lr 仍为 2.5e-6 (BUG-69 未生效)

## 启动前检查清单 (BUG-68 流程改进)
- [ ] 确认 BUG-69 修复: adapt_layers lr_mult=1.0 在 config 中
- [ ] 确认 BUG-62 修复: clip_grad=50.0 在 config 中
- [ ] 训练开始后打印 param_groups lr 确认
- [ ] @2000 后立即运行 check_frozen_predictions.py

## 注意
- Critic 已被授予紧急停止权 — 如果诊断确认 frozen, Critic 可直接 kill
- @2000 bg_FA > 0.7 即 STOP, 不等 @4000

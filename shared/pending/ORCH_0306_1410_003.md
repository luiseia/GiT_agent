# ORCH 指令 — 003
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: P1 最终评估 + P2 准备与启动 (BUG-9 修复)

## 任务描述
P1 训练已完成 (6000/6000)。ORCH_002 BUG-9 诊断已验收，Conductor 批准 max_norm=10.0 方案。现在需要：
1. 确认 P1 最终指标
2. 准备并启动 P2 训练

## 需要做的事情

### 任务 1: P1@6000 最终评估 (用 BUG-12 修正后的 eval)
用当前 repo 中的 eval 代码（已含 BUG-12 修复）对 `iter_6000.pth` 做一次独立评估：
```bash
cd /home/UNT/yz0370/projects/GiT
python tools/test.py configs/GiT/plan_d_reg_w1.py \
  ssd_workspace/Train/Train_20260305/plan_d_center_around/iter_6000.pth \
  --gpu-id 0
```
记录精确的 truck/bus/car/trailer recall/precision、bg_false_alarm、offset 指标。

### 任务 2: 更新 plan_e config
修改 `configs/GiT/plan_e_bug9_fix.py`：
1. `clip_grad=dict(max_norm=10.0, norm_type=2)` — 从 5.0 改为 **10.0** (Conductor 批准)
2. `load_from` 改为 `ssd_workspace/Train/Train_20260305/plan_d_center_around/iter_6000.pth`
3. 其他参数保持不变（center/around 权重、loss 权重等均沿用 P1）
4. 更新文件顶部注释说明变更

### 任务 3: 启动 P2 训练
在 GPU 0,2 上启动 P2 训练（确认 GPU 空闲后再启动）：
```bash
cd /home/UNT/yz0370/projects/GiT
CUDA_VISIBLE_DEVICES=0,2 nohup python -m torch.distributed.launch \
  --nproc_per_node=2 --master_port=29501 \
  tools/train.py configs/GiT/plan_e_bug9_fix.py \
  --work-dir ssd_workspace/Train/Train_20260306/plan_e_bug9_fix \
  --seed 2025 \
  > ssd_workspace/Train/Train_20260306/plan_e_bug9_fix/train.log 2>&1 &
```
（注意：先创建输出目录）

## 验收标准
1. P1@6000 精确指标记录（用修正 eval）
2. plan_e config 已更新且通过语法检查
3. P2 训练已启动，前 50 iter 日志无报错
4. 结果写入 `shared/logs/report_ORCH_003.md`

## 注意
- P2 的唯一变量是 max_norm: 0.5 → 10.0，其他全部沿用 P1
- load_from 加载模型权重但重置优化器状态 — 这是正确的，因为旧优化器状态是在 Sign-SGD 下积累的
- 关注 P2 前 200 iter 的 grad_norm 分布，确认裁剪率降低
- 如果 P2 出现训练不稳定（loss 爆炸），先停止训练，报告问题

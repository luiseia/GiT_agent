# ORCH_008 — P5: DINOv3 中间层特征集成 + 训练
> 签发: claude_conductor
> 时间: 2026-03-07 17:00
> 循环: #40 (Phase 2 触发: avg_P=0.107 < 0.12)
> 目标: claude_admin
> 状态: COMPLETED
> 优先级: URGENT

## 背景
P4 完成, AABB 修复提升 Recall 但 Precision 仍是瓶颈 (avg_P=0.107)。Critic VERDICT_P4_FINAL 确认: Precision 瓶颈根因是 DINOv3 Conv2d (Layer 0) 只编码纹理/边缘, 缺乏类别语义。Phase 2 触发条件已满足。DINOv3 特征已预提取就绪。

## 任务清单

### 任务 1: PreextractedFeatureEmbed 实现
**文件**: `GiT/mmdet/models/backbones/vit_git.py`

实现 `PreextractedFeatureEmbed` 类:
```python
class PreextractedFeatureEmbed(nn.Module):
    """Load pre-extracted DINOv3 features and project to model dim."""
    def __init__(self, feature_dir, layer_key='layer_16',
                 in_dim=4096, out_dim=768):
        super().__init__()
        self.feature_dir = feature_dir
        self.layer_key = layer_key
        self.proj = nn.Linear(in_dim, out_dim)

    def forward(self, img, img_metas=None, **kwargs):
        # 从 img_metas 获取 nuScenes token, 加载对应 .pt 文件
        # 返回: (batch, num_patches, out_dim)
        ...
```

**关键要求**:
1. 从 `img_metas` 获取图像标识 (nuScenes token 或文件名), 映射到 `.pt` 文件路径
2. 加载预提取特征, 选择 `layer_key` 对应层
3. FP16→FP32 转换 (如需要)
4. 通过 `self.proj` 线性投影 (4096→768)
5. 使用 **Layer 16** (Critic 推荐: 平衡细节和语义)
6. `self.proj` 初始化: **随机初始化** (kaiming), 不复用旧 Conv2d 投影 (分布不同)

**向后兼容**: 通过 config 参数 `use_preextracted_features=True` 控制。`False` 时走原 DINOv3PatchEmbedWrapper 路径。

### 任务 2: Dataset 适配
**文件**: 需要在 dataset pipeline 或 data dict 中传递图像标识信息

确保 `img_metas` 中包含可用于定位 `.pt` 文件的标识:
- 优先: nuScenes `sample_token` (直接匹配 .pt 文件名)
- 备选: 图像文件名解析

### 任务 3: P5 Config 创建
**文件**: 创建 `plan_h_dinov3_layer16.py`

| 参数 | P4 (plan_g) | **P5 (plan_h)** | 原因 |
|------|-------------|-----------------|------|
| load_from | P3@3000 | **P4@500** | Critic: 对旧分布适应最浅 |
| use_preextracted_features | False | **True** | DINOv3 集成 |
| preextracted_feature_dir | — | `/mnt/SSD/GiT_Yihao/dinov3_features/` | — |
| preextracted_layer_key | — | `layer_16` | Critic: 平衡细节和语义 |
| max_iters | 4000 | **6000** | 新特征需更多训练 |
| warmup | 500 | **1000** | Critic: 特征分布差异大 |
| milestones | [2500, 3500] | **[4000, 5500]** | 适配 6000 iter |
| bg_balance_weight | 2.0 | **2.5** | Critic: 2.0 在 full LR 时 bg_FA 偏高 |
| reg_loss_weight | 1.5 | **1.5** | 保持 |
| use_rotated_polygon | True | **True** | 保持 AABB 修复 |
| base_lr | 5e-05 | **5e-05** | 保持 |
| max_norm | 10.0 | **10.0** | 保持 |
| val_interval | 500 | **500** | — |
| GPU | 0,2 | **0,2** | CEO 限制 |

**P4@500 checkpoint 路径**: `/mnt/SSD/GiT_Yihao/Train/Train_20260306/plan_g_aabb_fix/iter_500.pth`

### 任务 4: BUG-16 评估
**Critic 发现**: 预提取特征与数据增强不兼容。

请检查当前 train_pipeline 是否包含图像级数据增强 (flip, rotation, color jitter 等):
- 如果无增强 → BUG-16 不阻塞, 直接进行
- 如果有增强 → 需要决策: (a) 临时禁用增强, (b) 预提取 flip 版本, (c) feature-level 增强

### 任务 5: 启动 P5 训练
1. 确认代码修改通过基本测试 (前 10 iter 无报错)
2. 启动训练: `CUDA_VISIBLE_DEVICES=0,2`
3. 记录前 50 iter 的 loss/grad_norm

## 报告要求
完成后提交 `shared/logs/report_ORCH_008.md`:
1. PreextractedFeatureEmbed 代码 diff
2. Dataset 适配 diff
3. BUG-16 评估结果
4. P5 config 参数
5. P5 PID, GPU 分配
6. 前 50 iter loss/grad_norm
7. 特征加载验证 (确认 shape, dtype)

## 红线
- 特征加载后 shape 必须为 (batch, 4900, 768) — 与 Conv2d PatchEmbed 输出一致
- 前 50 iter 无 NaN/OOM
- 只使用 GPU 0,2
- 不修改评估逻辑 (只改训练时的特征输入)

# ORCH 指令 — 050

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 修复 ORCH_049 的双向 mode collapse 问题：温和回调 FG/BG 比（从 1x 到 ~3.3x）+ 引入 marker step grid_pos_embed dropout 打破空间先验 shortcut
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, `GiT/mmdet/models/dense_heads/git_occ_head.py`（或 `git.py` 中 grid_pos_embed 注入位置）
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**，通过 frozen-check 前不得跑完整验证

## 背景

ORCH_048 → ORCH_049 的 FG/BG 比变化：
- ORCH_048: `marker_pos_punish=3.0`, `bg_balance_weight=2.5` → FG/BG=**6x** → all-positive collapse
- ORCH_049: `marker_pos_punish=1.0`, `bg_balance_weight=5.0` → FG/BG=**1x** → all-negative collapse (@500: 0/1200 positive, TP=0)
- 结论: 需要居中值，同时用 grid_pos_embed dropout 打破 BUG-75 空间先验

## 实施要求

### P0: 修正 FG/BG 比（BUG-76）
- `marker_pos_punish`: `1.0` → **`2.0`**
- `bg_balance_weight`: `5.0` → **`3.0`**
- 新 FG/BG 比: (5×2.0) / (3.0×1.0) = **3.3x**（居于 6x 和 1x 之间）

### P0: 引入 marker grid_pos_embed dropout（BUG-75）
- 新增配置项: `marker_grid_pos_dropout=0.5`
- **仅在训练模式下**，对 occupancy 的 `pos_id=0`（marker step）以 50% 概率将 `grid_pos_embed` 置零
- **不影响** `pos_id>0` 的后续 class/box token — 它们仍正常使用 grid_pos_embed
- **不影响** 推理模式 — 推理时 grid_pos_embed 始终正常
- 实现参考位置: `grid_pos_embed` 被加到 input 的地方（可能在 `git_occ_head.py` 的 decoder 或 `git.py` 的 forward_transformer）

### P1: 保留已验证配置
- `RandomFlipBEV only`
- `grid_assign_mode='center'`
- `prefix_drop_rate=0.5`
- `around_weight=0.0`

### P1: 早停规则收紧（OR 触发 + 全阴性检测）

**@200 早停**（任一触发即 STOP）:
- `marker_same > 0.97` → STOP
- `saturation > 0.90` → STOP（all-positive 检测）
- `saturation < 0.05` → STOP（all-negative 检测，新增）

**@500 早停**（任一触发即 STOP）:
- `Positive IoU > 0.95` → STOP
- `Marker same > 0.90` → STOP
- `Saturation > 0.90` → STOP
- `Saturation < 0.05` → STOP（新增）

### P1: 双阶段 frozen-check
- `iter_200` 生成后立即运行 quick frozen-check
- `iter_500` 生成后运行完整 frozen-check
- 可视化输出:
  - `/home/UNT/yz0370/projects/GiT_agent/shared/logs/VIS/050_iter200_frozen_check`
  - `/home/UNT/yz0370/projects/GiT_agent/shared/logs/VIS/050_iter500_frozen_check`

## 验收标准

1. Config 中明确可见:
   - `marker_pos_punish=2.0`
   - `bg_balance_weight=3.0`
   - `marker_grid_pos_dropout=0.5`（或等效配置项）
2. 代码中 grid_pos_embed dropout 仅作用于训练时 marker step (`pos_id=0`)
3. `iter_200` frozen-check 已执行并给出数值
4. 若进入 `iter_500`，frozen-check 完整执行
5. 最小成功信号（相对 ORCH_049）:
   - `saturation` 在 `0.10 ~ 0.80` 之间（不再陷入两极）
   - `marker_same < 0.95`
   - `TP > 0`
6. 失败即停，不继续 full val

## 截止时间
- 05:00 CDT

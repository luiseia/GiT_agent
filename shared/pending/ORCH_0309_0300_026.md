# ORCH_026: Plan Q — 单类 Car 诊断实验 (Mini)

- **签发**: Conductor (Cycle #107)
- **优先级**: HIGH
- **状态**: COMPLETED — 全部 Task 完成, car_P@best=0.083 < 0.12 → 类竞争无关
- **目标**: 干净回答 "类竞争是否为 car_P 瓶颈"
- **背景**: Plan K 因 BUG-27 (num_vocal=221 vs 230 不兼容) 结论无效。CEO 要求重新设计。

---

## Task 1: 创建 Plan Q Config

**文件**: `configs/GiT/plan_q_single_car_diag.py`

基于 `plan_full_nuscenes_gelu.py` 修改:

```python
# === 模型配置: 完全不改 ===
num_vocal = 230          # 与 ORCH_024 完全相同!
num_classes = 10         # 保持 10 类 slot (避免 BUG-27)
preextracted_proj_hidden_dim = 2048
preextracted_proj_use_activation = True  # GELU

# === 关键修改 1: 使用预提取特征 (非在线 DINOv3) ===
# 目的: 不加载 DINOv3 7B 模型, 节省 VRAM, 允许与 ORCH_024 共享 GPU
use_online_dinov3 = False
use_preextracted_features = True
preextracted_feature_dir = '/mnt/SSD/GiT_Yihao/dinov3_features/'
preextracted_layer_key = 'layer_16'

# === 关键修改 2: 数据过滤 — 只保留 car 标注 ===
# 在 GenerateOccFlowLabels 或 LoadAnnotations3D_E2E 中添加过滤:
# 只保留 gt_labels == 0 (car) 的标注, 其他类标注丢弃 (视为背景)
# 实现方式: 在 pipeline 中添加 class_filter 参数
# 或: 在 config 的 train_pipeline 中添加一个 FilterAnnotations step

# === 训练配置 ===
dataset = 'nuscenes_mini'
max_iters = 3000         # 充分收敛 (mini)
val_interval = 500
batch_size = 1           # 省 VRAM
accumulative_counts = 1  # mini 不需要
workers_per_gpu = 2

# === Checkpoint ===
load_from = '/mnt/SSD/GiT_Yihao/Train/Train_20260306/plan_i_p5b_3fixes/iter_3000.pth'
# P5b@3000, num_vocal=230, 完全兼容
```

**实现 class filter 的具体方法** (选其一):

方法 A: 在 `GenerateOccFlowLabels.__call__()` 开头添加:
```python
if hasattr(self, 'class_filter') and self.class_filter is not None:
    keep_mask = torch.tensor([c in self.class_filter for c in results['gt_labels_3d']])
    results['gt_bboxes_3d'] = results['gt_bboxes_3d'][keep_mask]
    results['gt_labels_3d'] = results['gt_labels_3d'][keep_mask]
```

方法 B: 在 train_pipeline 中添加独立的 filter step (更干净):
```python
dict(type='FilterGTByClass', keep_classes=[0])  # 0 = car
```

**选哪个由 Admin 判断，核心要求: 只保留 car 类 GT, 不改模型架构。**

## Task 2: 验证 Config 可用

```bash
# 单 GPU 测试 (选 ORCH_024 占用较轻的一卡)
CUDA_VISIBLE_DEVICES=1 python tools/train.py configs/GiT/plan_q_single_car_diag.py \
    --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260309/plan_q_single_car \
    --cfg-options runner.max_iters=10
```

验证:
1. Checkpoint 加载无 shape mismatch 警告
2. 10 iter 跑通无报错
3. 训练日志中只有 car 类 GT (无 truck/bus 等)
4. 显存 < 15 GB (确认不加载 DINOv3)

## Task 3: 运行完整训练

```bash
CUDA_VISIBLE_DEVICES=1 python tools/train.py configs/GiT/plan_q_single_car_diag.py \
    --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260309/plan_q_single_car
```

- 训练 3000 iter, val@500/1000/1500/2000/2500/3000
- 如果 VRAM 不够: 尝试 CUDA_VISIBLE_DEVICES=3 或减小 batch
- 如果 OOM: 报告并等待 CEO 决策

## Task 4: 结果提取

训练完成后提取 car_P/car_R 曲线, 写入 `shared/logs/report_ORCH_026.md`:

```
| iter | car_R | car_P | bg_FA | off_th |
|------|-------|-------|-------|--------|
| @500 | ? | ? | ? | ? |
| ...  | ... | ... | ... | ... |
| @3000| ? | ? | ? | ? |
```

对比基线: P6@4000 单GPU car_P=0.1263

---

**判定标准**:
- Plan Q car_P@best > 0.20 → 类竞争是主要瓶颈
- Plan Q car_P@best 0.15-0.20 → 类竞争是 contributing factor
- Plan Q car_P@best 0.12-0.15 → 类竞争不是瓶颈
- Plan Q car_P@best < 0.12 → 类竞争无关

---

*Conductor 签发 | 2026-03-09 ~03:00*

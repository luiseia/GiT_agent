# ORCH 指令 — 058

- **状态**: PENDING
- **优先级**: HIGH
- **目标**: 按 Critic 对 `ORCH057_MARKER_NO_POS` 的 CONDITIONAL 判决实现 `marker_step_no_pos`，并启动 2-GPU DDP frozen-check 训练
- **代码路径**: `GiT/mmdet/models/detectors/git.py`, `GiT/mmdet/models/dense_heads/git_occ_head.py`, `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`（如需新增显式开关）
- **关联审计**: `shared/audit/processed/VERDICT_ORCH057_MARKER_NO_POS.md`
- **截止时间**: 15:30

## 执行要求

### 1. 先落地 Critic 条件，不允许跳过

- 训练和推理两条路径都要处理 `marker_no_pos`，不能只改 `decoder_inference`
- `marker step (pos_id=0)` 移除 `grid_pos_embed`
- `class/box steps` 保留 `grid_pos_embed`
- 需要显式保留并传递 `grid_pos_embed_raw`，避免把“减去位置编码”写成只在单一路径生效的临时逻辑

### 2. 推荐实现口径

- 推理侧按 Critic 建议，在 `git_occ_head.py` 对 `pos_id=0` 做定向减法，其他 step 保持原逻辑
- 训练侧同步处理 `grid_feature/grid_start_embed`，确保 marker 不吃 `grid_pos_embed`，但 box 相关 step 不丢空间信息
- 如果实现中需要增加显式开关，默认对 occupancy prediction 打开，训练/推理行为必须一致

### 3. 启动训练

- 基线配置沿用 `ORCH_055` 的 2-GPU DDP 健康点设置:
  - `marker_pos_punish=1.0`
  - `bg_balance_weight=5.0`
  - `marker_grid_pos_dropout=0.0`
- 必须 2-GPU DDP，禁止单卡替代
- work_dir 统一命名为 `full_nuscenes_large_v1_orch057`

### 4. Frozen-check 与停训规则

- 在 `@100/@200/@300/@400/@500` 全部保存 checkpoint 并执行 frozen-check
- 任一点若满足以下任一条件，立即停训并提交报告:
  - `Positive IoU > 0.95`
  - `marker_same > 0.95`
  - `Saturation > 0.95`

## 验收标准

1. 报告中必须给出代码修改摘要，明确训练侧与推理侧各改了哪些位置
2. `marker_step_no_pos` 仅影响 marker step，不得把 class/box 的位置编码一并删除
3. 训练成功拉起为 2-GPU DDP，并附上进程/GPU 证据
4. `@100` frozen-check 的 `marker_same` 必须低于 `0.887`
5. 如 `@200 marker_same < 0.95` 且未触发 frozen 停训，报告中标记为“首次 @200 HEALTHY”

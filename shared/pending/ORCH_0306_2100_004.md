# ORCH 指令 — 004
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: BUG-8 修复 + BUG-10 修复 + P3 训练启动
- **审计依据**: VERDICT_P2_FINAL (CONDITIONAL)

## 任务清单 (按顺序执行)

### 任务 1: BUG-8 修复 — cls loss 添加背景类

**文件**: `GiT/mmdet/models/dense_heads/git_occ_head.py`

**修改点 A — Focal 路径 (约 L886-896):**
在 `for c in range(self.num_classes)` 循环结束后、`if total_weight > 0` 归一化之前，添加背景 cls loss:

```python
    # >>> BUG-8 FIX: 添加背景 cls loss <<<
    bg_cls_mask = (cls_target_rel == self.num_classes) & (cls_weight > 0)
    if bg_cls_mask.any():
        loss_cls = loss_cls + self.bg_balance_weight * (c_focal[bg_cls_mask] * cls_weight[bg_cls_mask]).sum() / cls_weight[bg_cls_mask].sum().clamp_min(1.0)
        total_weight += self.bg_balance_weight
```

**修改点 B — CE 路径 (约 L952-963):**
同样在 `for c in range(self.num_classes)` 循环结束后添加:

```python
    # >>> BUG-8 FIX <<<
    bg_cls_mask = (cls_target_rel == self.num_classes) & (cls_weight > 0)
    if bg_cls_mask.any():
        loss_cls = loss_cls + self.bg_balance_weight * c_weighted[bg_cls_mask].sum() / cls_weight[bg_cls_mask].sum().clamp_min(1.0)
        total_weight += self.bg_balance_weight
```

**注意**: 两处的归一化需改为 `total_weight`，与 marker loss (L853-867) 保持一致。请对照 marker loss 的 bg_mask 处理确认模式正确。

### 任务 2: BUG-10 修复 — P3 config 加 warmup

**文件**: 创建 `GiT/configs/GiT/plan_f_bug8_fix.py` (复制 plan_e_bug9_fix.py 并修改)

修改 `param_scheduler`:
```python
param_scheduler = [
    dict(
        type='LinearLR',
        start_factor=0.001,
        by_epoch=False,
        begin=0,
        end=500
    ),
    dict(
        type='MultiStepLR',
        by_epoch=False,
        begin=500,
        end=4000,        # P3 总步数 4000
        milestones=[2500, 3500],
        gamma=0.1
    )
]
```

其他 config 变更:
- `max_iters = 4000` (Critic 建议)
- `load_from = P2@6000 checkpoint 路径`
- `resume = False`
- `checkpoint_interval = 500`
- `val_interval = 500`
- 保持 `max_norm = 10.0`, `bg_balance_weight = 3.0`

### 任务 3: 启动 P3 训练

- GPU: 0,2 (或 0,1,2,3 如果 4 GPU 可用)
- 工作目录: `/mnt/SSD/GiT_Yihao/Train/Train_20260306/plan_f_bug8_fix/`
- 启动后报告 PID 和前 50 iter 的 loss/grad_norm

## 验收标准
1. BUG-8 修复代码通过语法检查
2. P3 config 创建完成，warmup 配置正确
3. P3 训练正常启动，前 50 iter 无 NaN/OOM
4. **关键监测**: avg_precision 是否在前 1000 步显著上升 (Critic 预期)
5. 报告写入 `shared/logs/report_ORCH_004.md`

## 截止时间
- BUG-8 + BUG-10 修复: 1 小时内
- P3 启动: 修复后立即

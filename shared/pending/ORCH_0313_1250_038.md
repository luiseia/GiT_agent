# ORCH 指令 — 038: 恢复 ORCH_035 训练 (从 iter_12000 resume)
- **状态**: COMPLETED
- **优先级**: CRITICAL — 训练已停止, 尽快恢复
- **目标**: 恢复 ORCH_035 训练, 从最近 checkpoint resume
- **截止时间**: ORCH_036 eval 完成后立即执行

## 背景
- ORCH_036 score_thr 消融的 4 个 eval 进程占满 4 GPU, 导致训练 OOM 崩溃
- 训练停止在 iter 12160 (2026-03-13 12:04)
- 需要在 eval 完成后 (预计 ~13:01) 恢复训练

## 前置条件
**必须等待 ORCH_036 的 4 个 eval 进程全部完成后再执行!**
```bash
# 检查 eval 是否完成
while ps aux | grep "tools/test.py" | grep -v grep | grep -q .; do
  echo "eval 仍在运行, 等待..."
  sleep 60
done
echo "eval 已完成, 可以恢复训练"
```

## 执行步骤

### 1. 确认 GPU 空闲
```bash
nvidia-smi --query-gpu=index,memory.used --format=csv,noheader
# 所有 GPU memory.used 应 < 1000 MiB
```

### 2. 找到最近 checkpoint
```bash
ls -lt /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v4/iter_*.pth | head -3
```

### 3. 恢复训练
```bash
cd /home/UNT/yz0370/projects/GiT
conda activate GiT

python -m torch.distributed.launch --nproc_per_node=4 --master_port=29500 \
  tools/train.py configs/GiT/plan_full_nuscenes_multilayer.py \
  --resume /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v4/iter_12000.pth \
  --launcher pytorch
```

**注意**: 使用原始 config 和 --resume flag, 训练将从 iter 12000 继续 (iter 12001-12160 的进度将重新训练, 损失极小)

### 4. 确认恢复成功
```bash
# 等待 1 分钟后检查日志
sleep 60
tail -3 /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v4/20260312_175353/20260312_175353.log
# 应看到新的 Iter(train) 条目
```

### 5. 写报告
输出到 `shared/logs/report_ORCH_038.md`:
- 恢复时间
- 恢复的 checkpoint 和 iter
- 确认训练正常运行

## 教训记录
- 并行 eval 任务必须用 `CUDA_VISIBLE_DEVICES` 指定单个 GPU
- 4 个 eval 应串行执行 (1 GPU) 或明确分配到空闲 GPU
- 训练占用 4 GPU 时, eval 只能在训练 val 期间 (GPU 内存较低时) 执行

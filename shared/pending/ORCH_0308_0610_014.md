# ORCH 指令 — 014
- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: P6 完整 nuScenes 训练准备
- **背景**: P5b 即将完成 (~06:33), CEO 战略转向完整 nuScenes. 10 类代码已 commit (GiT `2b52544`), BUG-19 已修复.

## 任务清单

### 1. 数据调查
- 检查完整 nuScenes 数据集是否存在 (不是 mini)
  - pkl 文件: `data/infos/` 或 `data/` 下是否有 `nuscenes_infos_temporal_train.pkl` (非 mini 版)
  - 图像数据: CAM_FRONT 等 6 个相机的完整图像集
  - 报告: 样本数量, 标注框数量, 磁盘占用

### 2. DINOv3 特征
- 当前 mini 特征: `/mnt/SSD/GiT_Yihao/DINOv3_features/` (323 files, 24GB)
- 调查: 完整 nuScenes 需要多少图像的 DINOv3 特征? 预估存储和时间
- 特征提取脚本位置: 确认可用并报告

### 3. 磁盘空间
- 检查 `/mnt/SSD/` 和 `/home/UNT/yz0370/` 剩余空间
- 评估完整 nuScenes 特征 + checkpoint 所需空间

### 4. P6 Config 草案
- 基于 `plan_i_p5b_3fixes.py` 创建 `plan_j_full_nuscenes.py`
- 关键修改:
  - `load_from`: P5b@3000 checkpoint 路径
  - `ann_file`: 完整 nuScenes pkl 路径
  - `classes`: 已是 10 类 ✅
  - `max_iters`: 待定 (根据数据量调整)
  - DINOv3 特征路径: 完整版

### 5. Checkpoint 兼容性验证
- 用 P5b@3000 实际加载测试 (dry run, 不训练)
- 确认 10 类词表 (num_vocal=230) 与 4 类 checkpoint (num_vocal=224) 的加载行为
- 报告: 哪些权重被跳过, 是否有 warning

- **代码路径**: GiT/configs/GiT/, GiT/mmdet/, GiT/data/
- **验收标准**:
  1. 完成数据/特征/磁盘调查报告
  2. P6 config 草案创建
  3. Checkpoint 加载 dry run 通过
- **截止时间**: P5b 完成后 2 小时内

---

## 执行结果 (Admin Agent, 2026-03-08 06:30)

### 1. 数据调查 ✅

| 项目 | Mini | Full |
|------|------|------|
| pkl 路径 | `data/infos_mini/` | `data/infos/` |
| Train 样本 | 323 | **28,130** |
| Val 样本 | — | **6,019** |
| GT boxes (train) | ~3K | **974,146** |
| CAM_FRONT 图像 | 34,149 | 34,149 (硬链接共享) |
| 图像路径 | `data/nuscenes_mini/` | `data/nuscenes/` |

图像数据: `data/nuscenes/` 和 `data/nuscenes_mini/` 共享相同文件 (inode 一致)，6 个相机完整。

### 2. DINOv3 特征 ⚠️ BLOCKER

| 项目 | 值 |
|------|------|
| Mini 特征 | 323 files, 25 GB |
| 每文件大小 | ~76.6 MB |
| Full train 需要 | 28,130 files ≈ **2,103 GB (~2.1 TB)** |
| 提取脚本 | `scripts/extract_dinov3_features.py` ✅ 可用 |

**⚠️ 存储 BLOCKER**: SSD 仅剩 528 GB，远不够 2.1 TB。
**解决方案建议**:
- (a) fp16 压缩 → ~1 TB (仍不够)
- (b) 使用更大存储 / NFS
- (c) 在线提取 (需要 DINOv3 模型常驻 GPU, 显存紧张)
- (d) 分批提取 + 流式训练

### 3. 磁盘空间 ✅

| 路径 | 总容量 | 已用 | 可用 | 使用率 |
|------|--------|------|------|--------|
| `/mnt/SSD/` | 3.7 TB | 3.0 TB | **528 GB** | 86% |
| `/home/` | 3.7 TB | 3.5 TB | **70 GB** | 99% |

SSD 主要占用: Train/ (769 GB), GiT_work_dirs/ (209 GB), Debug/ (167 GB)

### 4. P6 Config 草案 ✅

`configs/GiT/plan_j_full_nuscenes.py` 已创建 (GiT commit `b3cca77`)

关键参数:
- `load_from`: P5b@3000
- `data_root`: `data/nuscenes/`, `ann_file`: `data/infos/nuscenes_infos_temporal_train.pkl`
- 10 classes, `num_vocal=230`
- `max_iters=36000` (~20 epochs), `val_interval=2000`
- DINOv3 features dir: `TODO` (待解决 BLOCKER)

### 5. Checkpoint 兼容性 ✅

P5b@3000 checkpoint 分析:
- **267 个权重 key**
- **无** num_vocal=224 维度的权重 — vocab tokens 由 BERT embedding (30524×768) 动态索引
- 4→10 类扩展 **完全兼容**，无权重跳过，无 shape mismatch
- 唯一预期 warning: 如果新增 backbone layers 不在 checkpoint 中 (layers 12-17 已在 P5b 中训练)

**结论**: Checkpoint 加载无障碍，vocab 扩展透明。

### 关键决策点

**DINOv3 特征存储是唯一 BLOCKER**。需要 CEO 决策：
1. 清理 SSD (Debug/ 167 GB + old work_dirs 可回收 ~300 GB) → 仍不够
2. 使用外部存储
3. 改为在线 DINOv3 提取 (需要额外 GPU 显存)
4. 先用 mini 继续优化，延后 full nuScenes

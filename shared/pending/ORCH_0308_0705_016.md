# ORCH 指令 — 016
- **状态**: IN_PROGRESS
- **优先级**: HIGH
- **目标**: DINOv3 在线提取 + Unfreeze 实验 (方案 B, GPU 1,3)
- **背景**: CEO 决策放弃预提取路线, 走在线提取以支持完整 nuScenes 训练。GPU 1,3 空闲。Plan K/L 在 GPU 0,2 上运行中 (无冲突)。VERDICT_P6_ARCHITECTURE 已审计 (Critic 建议 B 为最后手段, 但 CEO override)。注: BUG-26 发现只用 CAM_FRONT, 预提取 175GB fp16 也可行, 但 CEO 选择在线路线以避免长期存储依赖。

## 任务清单

### 1. 实现 DINOv3 在线提取模块 (BUG-25 修复)
- **位置**: `GiT/mmdet/models/backbones/vit_git.py` (L91-150, `PreextractedFeatureEmbed`)
- **目标**: 新增 `OnlineDINOv3Embed` 类 (或重构为双模式):
  - 加载 DINOv3 ViT-G 权重 (fp16)
  - forward 时接收原始图像, 输出 layer 16 特征 (4096 维)
  - 与现有 `PreextractedFeatureEmbed` 共享投影层接口
- **DINOv3 权重路径**: 查找 `/mnt/SSD/GiT_Yihao/` 或项目中的 DINOv3 模型文件
- **显存预估**: DINOv3 ViT-G fp16 ~2.2GB 权重 + ~8GB 激活 ≈ 10GB (A6000 48GB 充足)
- **关键约束**:
  - DINOv3 参数默认 `requires_grad=False` (frozen)
  - 可选: 解冻最后 N 层 (N=2~4) 作为 unfreeze 模式
  - 确保与 GiT 的 data pipeline 兼容 (图像预处理)
- **向后兼容**: 保留 `PreextractedFeatureEmbed`, 通过 config 参数切换在线/离线模式

### 2. 创建 plan_m_online_dinov3_diag.py (实验 γ)
- 基于 `plan_k_car_only_diag.py` (单类 car, mini)
- **关键修改**:
  - backbone 切换为 `OnlineDINOv3Embed` 模式
  - DINOv3 权重路径
  - `classes = ["car"]`, `num_classes = 1` (与 plan_k 一致, 控制变量)
  - DINOv3 最后 2 层 unfreeze, LR 单独设低 (1e-6)
  - 投影层: Linear(4096,1024)+GELU+Linear(1024,768) (与 P5b 一致)
  - `max_iters = 2000`, `val_interval = 500`
- **GPU**: 1 (或 1+3 如果需要 DDP)

### 3. 创建 plan_n_online_frozen_diag.py (实验 δ, 对照)
- 与 plan_m 相同, 但 DINOv3 完全冻结 (requires_grad=False)
- **目的**: 与 plan_m 对比, 隔离 unfreeze 的效果
  - plan_m (unfreeze) vs plan_n (frozen online) → unfreeze 增益
  - plan_n (frozen online) vs plan_k (预提取) → 在线 vs 离线一致性验证
- **GPU**: 3

### 4. 启动实验
- Plan K/L 在 GPU 0,2 上运行中, 不要干扰
- 在 GPU 1 上启动 plan_m (online unfreeze)
- 在 GPU 3 上启动 plan_n (online frozen)
- 记录启动命令和 PID

### 5. 结果报告
- 四个诊断实验完成后汇总:
  - α (plan_k): 单类 car, 预提取, GPU 0
  - β (plan_l): 4 类宽投影, 预提取, GPU 2
  - γ (plan_m): 单类 car, 在线 unfreeze, GPU 1
  - δ (plan_n): 单类 car, 在线 frozen, GPU 3
- 汇总到 `shared/logs/admin_report_diag_full.md`

- **代码路径**: GiT/mmdet/models/backbones/vit_git.py, GiT/configs/GiT/
- **验收标准**:
  1. `OnlineDINOv3Embed` 模块实现并通过 forward 测试
  2. plan_m 和 plan_n 创建, 通过语法检查
  3. GPU 1,3 上启动实验
  4. 结果汇总报告
- **截止时间**: 尽快开始 (GPU 1,3 空闲中)

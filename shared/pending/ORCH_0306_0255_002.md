# ORCH 指令 — 002
- **状态**: COMPLETED
- **优先级**: CRITICAL
- **目标**: BUG-9 诊断 — 梯度裁剪 100% 问题分析与修复方案

## 任务描述
当前所有训练 config 使用 `clip_grad=dict(max_norm=0.5)`，但实测 grad_norm 范围 3.85-59.55，导致 100% 迭代被裁剪。AdamW 退化为 Sign-SGD，LR schedule 完全失效。

## 需要做的事情

### 阶段 1: 诊断（优先）
1. 读取 P1 训练日志，统计 grad_norm 分布（均值、中位数、P95、P99）
2. 读取当前 config `configs/GiT/plan_d_reg_w1.py`，确认 clip_grad 设置
3. 分析各 loss 组件的梯度贡献（marker/cls/reg 各占多少）

### 阶段 2: 方案设计（不执行，只报告）
评估以下修复选项，给出推荐方案：
- **选项 A**: 提高 max_norm 到 5.0 / 10.0 / 20.0
- **选项 B**: Loss scaling — 缩小 loss 的数值范围使梯度回到正常区间
- **选项 C**: 分层裁剪 — 对不同 loss 组件使用不同的 clip 阈值

## 验收标准
1. 完成 grad_norm 统计分析
2. 给出明确的推荐方案（附理由）
3. 结果写入 `shared/logs/report_ORCH_002.md`

## 注意
- **不要修改代码** — 本指令仅诊断，修复方案需 Conductor 审批后再执行
- 可以只读 GiT/ 中的代码和日志

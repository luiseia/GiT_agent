# ORCH_043: P2+P3 修复后从 iter_4000 重启训练

- **状态**: DELIVERED
- **优先级**: P0 — 立即执行
- **签发时间**: 2026-03-14 19:35
- **触发**: P2+P3 架构修复完成 + TF 诊断确认方向正确

## 背景

P2+P3 修复解决了 frozen predictions 的架构根因：
1. **P2** (`git.py:334`): occ 任务现在接收 position embedding，grid cell 不再全部相同起始
2. **P3** (`git_occ_head.py`): 每个解码步都注入 grid_token + image_interp_feats，不再只有第一步
3. **推理对称修复**: layer_id>0 重注入 grid_token，与训练路径一致

TF vs AR 诊断确认 @4k 的模型完全不使用自回归上下文（TF=AR），这不是 exposure bias 而是位置信息缺失。P2+P3 直接解决此根因。

## 任务 (严格按顺序)

### Step 1: 确认代码状态
```bash
cd /home/UNT/yz0370/projects/GiT
git log --oneline -5
# 确认包含 P2+P3 修复 commit (d9d7f7d) 和 clip_grad 回退 (c416818)
```

### Step 2: 确认 config 状态
```bash
# 确认 clip_grad=10 (不是 30)
grep -n "clip_grad" configs/GiT/plan_full_nuscenes_large_v1.py
# 确认 load_from 指向 iter_4000
grep -n "load_from\|resume" configs/GiT/plan_full_nuscenes_large_v1.py
```

### Step 3: 确认 GPU 可用
```bash
nvidia-smi
# 需要至少 2 GPU 空闲 (每个 ~27GB)
# 如果 4 GPU 可用，使用 4 GPU
```

### Step 4: 启动训练
```bash
cd /home/UNT/yz0370/projects/GiT

# 根据 GPU 可用情况选择:
# 4 GPU 可用:
nohup bash -c 'CUDA_VISIBLE_DEVICES=0,1,2,3 PORT=29500 \
  ./tools/dist_train.sh configs/GiT/plan_full_nuscenes_large_v1.py 4 \
  --resume /mnt/SSD/GiT_Yihao/Train/Train_20260314/full_nuscenes_large_v1/iter_4000.pth' \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260314/nohup_large_v1_p2p3.out 2>&1 &

# 或 2 GPU:
nohup bash -c 'CUDA_VISIBLE_DEVICES=0,2 PORT=29500 \
  ./tools/dist_train.sh configs/GiT/plan_full_nuscenes_large_v1.py 2 \
  --resume /mnt/SSD/GiT_Yihao/Train/Train_20260314/full_nuscenes_large_v1/iter_4000.pth' \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260314/nohup_large_v1_p2p3.out 2>&1 &

echo "PID: $!"
```

### Step 5: 验证训练启动
1. 确认训练从 iter 4001 开始
2. 前 50 iter 的 loss 和 grad_norm 是否正常 (P2+P3 改变了计算图，loss 可能有初始跳变)
3. 确认日志输出到新文件 `nohup_large_v1_p2p3.out`
4. 记录 PID

## 报告
完成后写报告到 `shared/logs/report_ORCH_043.md`，包含:
- GPU 数量和 PID
- 前 20 iter 的 loss/grad_norm (P2+P3 修复前后对比)
- 是否有初始 loss 跳变 (预期: 有，因为架构变化)

## ⚠️ 注意事项
- **日志必须用新文件名** `nohup_large_v1_p2p3.out`，不要覆盖旧日志
- P2+P3 是架构级改动，前几百 iter 的 loss 可能异常，这是正常的
- 如果 loss 发散 (>20 持续 100+ iter)，先停训练通知 Conductor
- val@2000 保持不变 (iter_6000 会是第一个 P2+P3 eval)

# ORCH 指令 — 034: Kill ORCH_033 + BUG-52 IoF/IoB 修复后重启
- **状态**: PENDING
- **优先级**: HIGH
- **目标**: 部署 BUG-52 IoF/IoB 过滤修复, 用修复后代码重启训练
- **截止时间**: 03/12 06:00
- **依据**: CEO 指令 — 要求部署 Stage 2 IoF/IoB 过滤

## 背景

CEO 不确认 convex hull 等效 IoF/IoB，要求修复 BUG-52。代码已修改：
- `generate_occ_flow_labels.py` L387-401: convex hull 分支内追加 IoF/IoB 双重过滤
- 当 `min_iof>0 or min_iob>0` 时，cell 必须同时通过 convex hull center-check 和 `IoF≥0.30 OR IoB≥0.20`
- 向后兼容：无 IoF/IoB 参数时行为不变

由于标签在线生成，ORCH_033 使用的是修复前代码，必须重启。

## Task 1: 同步 BUG-52 修复代码

1. 在 GiT 项目中 `git pull` 获取最新代码
2. 确认 `generate_occ_flow_labels.py` 中 L387-401 的 BUG-52 fix 存在:
```python
if inside[idx]:
    if use_iof_iob_filter:
        # BUG-52 fix: convex hull + IoF/IoB 双重过滤
        ...
```
3. 清除字节缓存: `find . -name '*.pyc' -delete`

## Task 2: Kill ORCH_033

1. `kill $(pgrep -f "plan_full_nuscenes_multilayer.py")`
2. 确认 GPU 释放
3. **保留 ORCH_033 的 checkpoint 和日志** (对比用)

## Task 3: 重启训练

- Config 不需要改动 — `plan_full_nuscenes_multilayer.py` 已有:
  - `min_iof=0.30, min_iob=0.20` ✅ (参数一直存在，只是之前是死代码)
  - `load_from=ORCH_029@2000` ✅ (ORCH_033 已设置)
  - `proj_hidden_dim=4096` ✅
  - `clip_grad=30.0` ✅
  - `lr_mult=5.0` ✅

- 创建新工作目录: `mkdir -p /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v3`
- 启动训练:
```bash
cd /home/UNT/yz0370/projects/GiT
CUDA_VISIBLE_DEVICES=0,1,2,3 nohup python -m torch.distributed.launch \
  --nproc_per_node=4 \
  tools/train.py configs/GiT/plan_full_nuscenes_multilayer.py \
  --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v3 \
  --launcher pytorch \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260312/full_nuscenes_multilayer_v3/train_stdout.log 2>&1 &
```

## Task 4: 验证

等 2 分钟确认:
1. 4 GPU 进程正常
2. Partial load 正确 (仅 proj.0/proj.2 跳过)
3. 日志中首 iter loss ≈ 4.0 (与 ORCH_033 类似, 说明暖启动有效)
4. 写报告到 `shared/logs/report_ORCH_034.md`

## 验收标准
- ORCH_033 已停止
- BUG-52 修复代码已部署 (generate_occ_flow_labels.py 包含 IoF/IoB 双重过滤)
- ORCH_034 训练正常运行

# ORCH_017: P6 宽投影 mini 验证
- 状态: DELIVERED
- 签发: Conductor | Cycle #70
- 时间: 2026-03-08 09:10
- 优先级: HIGH
- GPU: 0 + 2

## 背景
VERDICT_DIAG_FINAL 批准宽投影 2048, 定稿 P6 Config。Plan K/L 诊断完成, P6 进入 mini 验证阶段。

## 任务

### Task 1: 创建 P6 Config
- 文件: `configs/GiT/plan_p6_wide_proj.py`
- 基于: `plan_i_p5b_3fixes.py`
- **关键修改**:
  1. **投影层**: `preextracted_proj_hidden_dim = 2048`
  2. **投影结构**: `nn.Sequential(nn.Linear(4096, 2048), nn.Linear(2048, 768))` — **无 GELU, 无 LayerNorm!**
     - 位置: `GiT/mmdet/models/backbones/vit_git.py:L104-109`
     - 当前代码: `nn.Sequential(nn.Linear(4096, 1024), nn.GELU(), nn.Linear(1024, 768))`
     - 修改为: `nn.Sequential(nn.Linear(4096, 2048), nn.Linear(2048, 768))`
     - **注意**: 如果 hidden_dim 是 config 参数, 只改 config 即可; 如果 GELU 是硬编码的, 需要修改代码使其可配置
  3. **投影层 LR 倍率**: `backbone.patch_embed.proj lr_mult = 2.0`
     - 通过 `paramwise_cfg` 设置: `paramwise_cfg=dict(custom_keys={'backbone.patch_embed.proj': dict(lr_mult=2.0)})`
  4. **load_from**: P5b@3000 checkpoint 路径
     - `/mnt/SSD/GiT_Yihao/Train/Train_20260307/plan_i_p5b_3fixes/iter_3000.pth`
  5. **保持不变**: num_vocal=230, 10 类, balance_mode='sqrt', bg_balance_weight=2.5
  6. **训练参数**: max_iters=6000, warmup=500, val_interval=500
  7. **LR milestones**: [2000, 4000] (相对 begin=500, 实际 decay @2500, @4500)

### Task 2: 修改投影层代码 (如需要)
- **检查** `vit_git.py:L104-109` 是否支持通过 config 控制 hidden_dim 和是否使用 GELU
- 如果 GELU 是硬编码的, 需要添加参数控制:
  ```python
  # 建议修改方式 (向后兼容):
  if self.preextracted_proj_hidden_dim:
      layers = [nn.Linear(4096, self.preextracted_proj_hidden_dim)]
      if getattr(self, 'proj_use_activation', True):  # 默认 True 保持向后兼容
          layers.append(nn.GELU())
      layers.append(nn.Linear(self.preextracted_proj_hidden_dim, 768))
      self.preextracted_proj = nn.Sequential(*layers)
  ```
  - P6 config 中设置: `proj_use_activation = False`
  - 旧 config 不设这个参数, 默认 True, 保持兼容

### Task 3: 启动 P6 训练
- **时机**:
  - 优先: 等 Plan M/N @1000 数据 (~09:22), 如果 M_car_P > 0.077 则在线路径有价值, 可能需要调整
  - 最晚: 09:30 前无论如何启动 (预提取路径风险更低)
- **GPU**: 0 + 2 (DDP)
- **验证**: 确认 config 正确, checkpoint 加载成功, proj 层 shape mismatch 自动随机初始化

### Task 4: P6 验证报告
- @500: 确认训练正常启动, loss 下降, proj 层 grad_norm 合理
- @1000: **关键检查点** — car_P ≥ 0.10 且 bg_FA ≤ 0.30 → PASS
- @2000: off_th 监控 — 无 GELU 是否恢复到 ≤0.18?
- @3000: 决定是否切 full nuScenes

## 验收标准
1. P6 config 文件创建完成, 能被 `python tools/train.py` 正确解析
2. 投影层确认为纯双 Linear (无 GELU 无 LN)
3. load_from P5b@3000, proj 层自动随机初始化
4. 训练正常启动, GPU 0+2 占用
5. 首次 val @500 数据报告

## 注意事项
- **向后兼容**: 修改代码时不能影响 P5b 和 Plan K/L/M/N 的已有 config
- **BUG-30 测试**: P6 是首个无 GELU 投影实验, off_th 数据至关重要
- **bg_balance_weight**: 初始 2.5, 如 @1000 bg_FA > 0.30 则下一轮提到 3.0

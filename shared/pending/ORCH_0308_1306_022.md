# ORCH_022: Plan P — 2048+GELU 宽投影验证 (最高优先级!)
- 状态: IN PROGRESS
- 执行时间: 2026-03-08 13:13 started
- 执行结果: Plan P config 创建, GPU 1 训练启动 (PID 644579), 等待 @500 结果 (~13:39)
- 签发: Conductor | Cycle #79
- 时间: 2026-03-08 13:06
- 优先级: **CRITICAL — BUG-39 修复验证, COND-2 BLOCKING full nuScenes**
- GPU: 1 (空闲)

## 背景

BUG-39 CRITICAL: P6 的 `Sequential(Linear(4096,2048), Linear(2048,768))` **无激活函数**数学上等价于单层 `Linear(4096,768)`。2048 维度不增加任何表达能力。P6 car_P=0.106 落后 P5b car_P=0.116 (-8.6%) 的根本原因。

**Plan P 是修复 BUG-39 后验证 2048 宽度真实价值的唯一实验。**

## Plan P 配置

从 `plan_p6_wide_proj.py` 复制并修改:

```python
# ★ 关键改动 (仅 2 处):
preextracted_proj_use_activation = True    # 恢复 GELU! (P6 是 False = BUG-39)
# proj lr_mult = 1.0                       # 从 2.0 改为 1.0 (分离变量)

# 其他与 P6 保持一致:
preextracted_proj_hidden_dim = 2048        # 保留 2048 宽度
classes = ['car','truck','bus','trailer','construction_vehicle',
           'pedestrian','motorcycle','bicycle','traffic_cone','barrier']
num_vocal = 230
load_from = '/mnt/SSD/GiT_Yihao/Train/Train_20260308/plan_i_p5b_3fixes/iter_3000.pth'
max_iters = 500                            # 快速验证
val_interval = 500                         # 仅最终 eval

# 其他参数同 P6:
balance_mode = 'sqrt'
bg_balance_weight = 2.5
warmup_iters = 500 → 改为 100 (500 iter 训练不需要 500 warmup)
```

**⚠️ 关键注意事项**:
1. **必须 `proj_use_activation=True`** — 这是整个实验的核心!不设 True = 重复 BUG-39
2. **lr_mult=1.0** — 不用 2.0, 与 P5b 保持一致, 分离变量
3. **不要用 P6 的 checkpoint** — P6 投影层学到的是线性映射, 对非线性架构无迁移价值. 必须从 P5b@3000 加载
4. **num_vocal=230** — 必须与 P6/P5b 一致, 避免 BUG-27
5. **单 GPU 训练** — 不用 DDP, 所有指标可信

## Task 1: 创建 Plan P config

config 文件名: `configs/GiT/plan_p_wide_gelu_verify.py`

从 P6 config (`plan_p6_wide_proj.py`) 复制, 修改:
- `preextracted_proj_use_activation = True` (关键!)
- proj 的 lr_mult 改为 1.0
- `max_iters = 500`
- `val_interval = 500`
- warmup 缩短 (100 iter)
- 确保 `load_from` 指向 P5b@3000

## Task 2: 启动训练

```bash
CUDA_VISIBLE_DEVICES=1 python tools/train.py \
    configs/GiT/plan_p_wide_gelu_verify.py \
    --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260308/plan_p_wide_gelu_verify
```

预计: ~3 s/iter × 500 = ~25 min (预提取比在线快)

## Task 3: 评估 @500 结果

**判定标准**:
- **car_P@500 > 0.073** (P6@500 水平) 且 **bg_FA < 0.200** → 2048+GELU 是 full nuScenes 最终 config!
- **car_P@500 > 0.10** 且 **bg_FA < 0.20** → 大成功, 宽投影 + GELU 远超 P5b
- **car_P@500 < 0.073** → 2048 宽度本身无价值, full 用 P5b config (1024+GELU)

**参考对比**:
| 实验 | proj_dim | GELU | lr_mult | car_P@500 | bg_FA@500 |
|------|----------|------|---------|-----------|-----------|
| P5b | 1024 | ✅ | 1.0 | 0.080 (DDP) | 0.235 |
| P6 | 2048 | ❌ | 2.0 | 0.073 | 0.173 |
| Plan L | 2048 | ✅ | 1.0 | 0.054 | 0.237 |
| **Plan P** | **2048** | **✅** | **1.0** | **?** | **?** |

> Plan L @500 car_P=0.054 较低, 但 @1000 飙升到 0.140 (历史最高). Plan L 有 BUG-28 (vocab 保留优势), Plan P 无此问题.

## 验收标准
1. Config 文件创建, `proj_use_activation=True` 已验证
2. 训练启动, 单 GPU, 日志正常
3. 500 iter 完成 + val 结果
4. 与判定标准对比, 给出 2048+GELU 是否优于 P5b 的结论

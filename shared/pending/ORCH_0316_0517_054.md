# ORCH 指令 — 054

- **状态**: DELIVERED
- **优先级**: HIGH
- **目标**: 精确复现 ORCH_049 配置 + 多点 frozen-check（@100/@200/@300/@400/@500）定位崩塌时间点
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, `GiT/scripts/auto_frozen_check_054.sh`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**
  5. **grid_pos_embed dropout 保持 0.0**

## 背景

ORCH_049 (marker_pos_punish=1.0, bg_balance_weight=5.0) 是唯一在 @200 存活的配置（TP=112），但在 @500 崩塌为全阴性。bg=3.0 和 bg=4.0 的实验均在 @200 就全阴性。

当前问题不是找可行 FG/BG 比，而是理解 **为什么 @200→@500 之间会崩塌**。需要多点数据来定位崩塌时间和轨迹。

## 实施要求

### Config — 精确复现 ORCH_049
- `marker_pos_punish=1.0`
- `bg_balance_weight=5.0`（从当前 4.0 改回 5.0）
- `marker_grid_pos_dropout=0.0`
- `around_weight=0.0`
- `grid_assign_mode='center'`
- `RandomFlipBEV only`
- `prefix_drop_rate=0.5`

### 多点 frozen-check（关键新增）

auto_frozen_check_054.sh 需要在每个检查点运行 frozen-check，**不提前停止**（除非全正饱和）：

- **@100**: 运行 frozen-check，记录结果，**继续训练**
- **@200**: 运行 frozen-check，记录结果，**继续训练**（即使 marker_same > 0.97）
- **@300**: 运行 frozen-check，记录结果，**继续训练**
- **@400**: 运行 frozen-check，记录结果，**继续训练**
- **@500**: 运行 frozen-check，记录结果，**停止训练**

**重要**: 只有 `saturation > 0.95`（全正饱和）才早停。全阴性和模板化允许继续，因为我们需要完整的崩塌轨迹。

每个检查点的可视化:
- `shared/logs/VIS/054_iter{100,200,300,400,500}_frozen_check`

### 报告要求

执行报告需包含 5 个检查点的完整数据表:

| iter | Pos slots | Positive IoU | marker_same | Coord diff | Saturation | TP |
|------|-----------|-------------|-------------|-----------|------------|-----|
| 100 | ? | ? | ? | ? | ? | ? |
| 200 | ? | ? | ? | ? | ? | ? |
| 300 | ? | ? | ? | ? | ? | ? |
| 400 | ? | ? | ? | ? | ? | ? |
| 500 | ? | ? | ? | ? | ? | ? |

## 验收标准

1. Config 精确匹配 ORCH_049: `marker_pos_punish=1.0`, `bg_balance_weight=5.0`
2. 5 个检查点全部执行 frozen-check（除非 @100 就全正饱和）
3. 每个检查点有完整指标数据和可视化
4. 报告中包含崩塌轨迹分析

## 截止时间
- 08:00 CDT

# ORCH_042: 修复 BUG-62/63/17 并从 iter_4000 Resume

- **状态**: DELIVERED
- **优先级**: P0 — Critic CONDITIONAL PROCEED 强制条件
- **签发时间**: 2026-03-14 10:45
- **触发**: VERDICT_LARGE_V1_AT4000 — CONDITIONAL PROCEED

## 背景

Critic 审计发现 GiT-Large v1 分类器未激活的首要原因是 BUG-62 (clip_grad=10.0 严重节流)。
必须停止当前训练，修复 config，从 iter_4000 checkpoint resume。

## 任务 (严格按顺序)

### Step 1: 停止当前训练
```bash
kill -SIGTERM <PID>  # PID 1169092, 或用 ps aux | grep plan_full_nuscenes
# 等待进程退出，确认 GPU 释放
nvidia-smi
```

### Step 2: 修改 Config — 三个修复
文件: `configs/GiT/plan_full_nuscenes_large_v1.py`

#### BUG-62 (CRITICAL): clip_grad 10 → 30
```python
# 找到 optim_wrapper 中的 clip_grad
# 改 max_norm=10 → max_norm=30
clip_grad=dict(max_norm=30, norm_type=2)
```

#### BUG-63 (MEDIUM): filter_invisible True → False
```python
# 找到 filter_invisible=True
filter_invisible=False
```

#### BUG-17 (BLOCKER): 激活 max_class_weight cap
```python
# 在 model.bbox_head 或 occ_head config 中添加:
max_class_weight=3.0
```
注意: 需确认 `max_class_weight` 参数在 `git_occ_head.py` 中的 `__init__` 已定义。参照 ORCH_037 代码。

### Step 3: 从 iter_4000 Resume
```bash
# 确认 checkpoint 存在
ls -la /mnt/SSD/GiT_Yihao/Train/Train_20260314/full_nuscenes_large_v1/iter_4000.pth

# 启动训练 (resume from iter_4000)
# 使用新的 work_dir 子目录以区分
cd /home/UNT/yz0370/projects/GiT
conda activate GiT

nohup bash -c 'CUDA_VISIBLE_DEVICES=0,1,2,3 PORT=29500 \
  ./tools/dist_train.sh configs/GiT/plan_full_nuscenes_large_v1.py 4 \
  --resume /mnt/SSD/GiT_Yihao/Train/Train_20260314/full_nuscenes_large_v1/iter_4000.pth' \
  > /mnt/SSD/GiT_Yihao/Train/Train_20260314/nohup_large_v1_resume4k.out 2>&1 &

echo "PID: $!"
```

### Step 4: 验证
1. 确认训练从 iter 4001 开始
2. 确认 grad_norm 不再被 clip 到 10 (应看到正常范围的 grad_norm)
3. 记录新 PID 到报告

## 报告
完成后写报告到 `shared/logs/report_ORCH_042.md`，包含:
- 修改的具体行号和 diff
- 新训练 PID
- 前 10 iter 的 grad_norm 对比（是否比修复前更大/正常）

## 注意事项
- resume 使用原 checkpoint 路径的 `iter_4000.pth`
- mmengine resume 会自动恢复 optimizer state，修改后的 clip_grad 会立即生效
- 日志输出到新文件 `nohup_large_v1_resume4k.out` 以便对比

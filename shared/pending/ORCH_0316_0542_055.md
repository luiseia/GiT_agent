# ORCH 指令 — 055

- **状态**: COMPLETED
- **优先级**: HIGH
- **目标**: 用 **2-GPU DDP** 精确复现 ORCH_049 + 多点 frozen-check 定位崩塌时间。ORCH_053/054 因单 GPU 执行导致结果无效(BUG-78)
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, `GiT/scripts/auto_frozen_check_055.sh`

## ⚠️ 训练启动方式（强制要求）

**必须用 2-GPU DDP，与 ORCH_049 完全一致：**

```bash
CUDA_VISIBLE_DEVICES=2,3 python -m torch.distributed.launch \
  --nproc_per_node=2 --master_port=29509 \
  tools/train.py configs/GiT/plan_full_nuscenes_large_v1.py \
  --work-dir /mnt/SSD/GiT_Yihao/Train/Train_20260316/full_nuscenes_large_v1_orch055 \
  --launcher pytorch
```

**不要用** `python tools/train.py`（单 GPU）！
**不要加** `--cfg-options` 覆盖 checkpoint interval（用 config 里的值）！

验证方式：
- 日志中应显示 `memory: 28878`（而非 27140）
- 应有 ~44s 的 accumulative step（而非全部 ~3.7s）
- ETA 应为 ~3-4 天（而非 ~1.7 天）

## 强制约束
1. **不要修改** `filter_invisible`，保持 `False`
2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
3. **不要恢复** `GlobalRotScaleTransBEV`
4. **full val 继续禁用**
5. **grid_pos_embed dropout 保持 0.0**

## Config — 精确复现 ORCH_049
- `marker_pos_punish=1.0`
- `bg_balance_weight=5.0`
- `marker_grid_pos_dropout=0.0`
- `around_weight=0.0`
- `grid_assign_mode='center'`
- `RandomFlipBEV only`
- `prefix_drop_rate=0.5`

## 多点 frozen-check（不提前停止，除非全正饱和）

@100, @200, @300, @400, @500 — 每个点运行 frozen-check，记录结果，继续训练。
仅 `saturation > 0.95` 才早停。

可视化: `shared/logs/VIS/055_iter{100,200,300,400,500}_frozen_check`

## 验收标准

1. 日志确认 2-GPU DDP（memory ~28878, accumulative steps ~44s）
2. Config 精确匹配 ORCH_049
3. @200 应复现 ORCH_049 的 TP>0（如果 ORCH_049 @200 TP=112 是可复现的）
4. 5 个检查点完整数据

## 截止时间
- 09:00 CDT

## 背景

ORCH_053(bg=4.0) 和 ORCH_054(bg=5.0) 都因 Admin 用单 GPU 启动而结果无效。
单 GPU 的 effective batch=1，而 ORCH_049 的 effective batch=16（2-GPU × accumulative_counts=8）。
16x batch size 差异导致训练动态完全不同，结果不可比。

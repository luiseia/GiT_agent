# ORCH 指令 — 051

- **状态**: PENDING
- **优先级**: HIGH
- **目标**: 隔离变量验证 — ORCH_050 失败因为同时改了 FG/BG 和 dropout，本轮只改 FG/BG=3.3x，关闭 dropout，确认 FG/BG=3.3x 能否复现 ORCH_049 @200 的 TP=112 并在 @500 避免崩塌
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`
- **强制约束**:
  1. **不要修改** `filter_invisible`，保持 `False`
  2. **不要改动** marker 训练路径 / teacher forcing 主逻辑
  3. **不要恢复** `GlobalRotScaleTransBEV`
  4. **full val 继续禁用**

## 背景

ORCH_050 同时改了 FG/BG (3.3x) 和 grid_pos_embed dropout (0.5)，结果比 ORCH_049 更差。
需要隔离确认 dropout 是罪魁祸首，FG/BG=3.3x 本身是否可行。

| ORCH | FG/BG | dropout | @200 TP | @200 saturation |
|------|-------|---------|---------|-----------------|
| 049 | 1x | 0 | 112 | 0.487 |
| 050 | 3.3x | 0.5 | 0 | 0.000 |
| **051** | **3.3x** | **0** | **?** | **?** |

## 实施要求

### 唯一改动: 关闭 grid_pos_embed dropout
- `marker_grid_pos_dropout`: `0.5` → **`0.0`**
- **其余所有配置保持与 ORCH_050 完全相同**:
  - `marker_pos_punish=2.0`
  - `bg_balance_weight=3.0`
  - `around_weight=0.0`
  - `grid_assign_mode='center'`
  - `RandomFlipBEV only`
  - `prefix_drop_rate=0.5`

### 双阶段 frozen-check
- iter_200 + iter_500
- 可视化: `shared/logs/VIS/051_iter200_frozen_check`, `shared/logs/VIS/051_iter500_frozen_check`

### 早停规则 (OR 触发)

@200:
- `marker_same > 0.97` → STOP
- `saturation > 0.90` → STOP
- `saturation < 0.05` → STOP

@500:
- `Positive IoU > 0.95` → STOP
- `Marker same > 0.90` → STOP
- `Saturation > 0.90` 或 `< 0.05` → STOP

## 验收标准

1. Config 中: `marker_grid_pos_dropout=0.0`
2. 其余 config 与 ORCH_050 完全一致
3. iter_200 frozen-check 执行
4. **期望结果** (如果 dropout 确实是主因):
   - @200: TP > 0, saturation 0.1-0.8 (类似 ORCH_049 @200)
   - 如 @200 通过，继续到 @500 验证 FG/BG=3.3x 是否避免 ORCH_049 @500 的全阴性崩塌
5. 失败即停

## 截止时间
- 05:00 CDT

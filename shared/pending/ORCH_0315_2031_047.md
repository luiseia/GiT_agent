# ORCH 指令 — 047

- **状态**: PENDING
- **优先级**: HIGH
- **目标**: 在 GiT 训练流水线中强制加入 CEO 要求的 `RandomFlipBEV` 和 `GlobalRotScaleTrans`，从零重启训练，验证是否能打破 `ORCH_046_v2 @500` 的 frozen predictions
- **代码路径**: `GiT/configs/GiT/plan_full_nuscenes_large_v1.py`, `GiT/mmdet/datasets/pipelines/`, `GiT/mmdet/datasets/`, `GiT/scripts/`
- **验收标准**:
  1. 新增可用的 `RandomFlipBEV`，在 `train_pipeline` 中位于 `LoadFrontCameraImageFromFile` / `PhotoMetricDistortion` 之后、`GenerateOccFlowLabels` 之前
  2. 新增可用的 `GlobalRotScaleTransBEV`，支持自动驾驶常用范围：
     - `rot_range=[-0.3925, 0.3925]`
     - `scale_ratio_range=[0.95, 1.05]`
  3. 变换必须同时作用到图像、3D boxes、heading，确保 `GenerateOccFlowLabels` 后的 BEV target 与增强后几何一致
  4. 提供 1-2 个样本的增强前后可视化或日志证明，确认 BEV 标签没有翻反/旋坏
  5. 从零启动新 work_dir，`val_interval=500` 保持不变
  6. `@500` 运行 `python scripts/check_frozen_predictions.py --config <cfg> --checkpoint <iter_500.pth>`，要求：
     - Positive IoU `< 0.95`
     - Marker same rate `< 0.90`
     - 不得再出现 `1200/1200` 全正饱和
- **截止时间**: 23:30
